# ─────────────────────────────────────────────────────────────
#  Godot 工程 · 版本控制忽略清单
#  【原则】只提交**源码与数据**（*.gd / *.tscn / *.tres / *.json / *.md），
#         引擎缓存、导出产物、本地调试开关一律不进仓库。
# ─────────────────────────────────────────────────────────────

# ── Godot 引擎缓存（打开工程会自动重建，绝不要提交）──
.godot/
# 老版本 Godot 的缓存目录名
.import/
*.translation

# ── 导出 / 构建产物 ──
build/
export/
*.exe
*.pck
*.zip
*.dmg
*.apk
*.aab
*.ipa
*.app/
# 导出配置里可能带本机路径 / 密钥，按需保留：这里默认忽略
export_presets.cfg

# ── 临时与备份文件 ──
*.tmp
*.temp
*.bak
*.orig
*.log
*~
*.swp
Thumbs.db
desktop.ini

# ── 本地开发用的一次性产物 ──
# 走路调试日志开关：存在这个文件时 waiter.gd 会打印寻路/到达详情
# （排查用，不该跟着仓库走；朋友要抓日志就自己建一个空文件）
DEBUG_WALK
# 截图与像素对比的产物（本地验证用，别人不需要）
.artifacts/

# ── 编辑器 / IDE ──
.vscode/
.idea/
*.sublime-*
