# 餐厅物语 · 进度（PROGRESS）

> 记录**当前实际状态**：做完了什么、卡在哪、还欠什么。
> 功能行为的权威描述见 `SPEC.md`。
>
> 最后更新：本轮（手上 2 份 + 橙汁 + 清洁判定修正完成后）

---

## 1. 一句话状态

**游戏可完整试玩、无阻塞性 bug**：接单 → 后厨下单 → 出餐口取餐 → 上菜 → 用餐 → 结账 →
收拾 → 客人离场，全链路跑通；客人气走、整组一起走、手上 2 份、桌边清洁判定都已按确认的
规则实现。

唯一的**已知技术债**是 `tools/run_tests.gd` 里几个分组的断言停留在旧设计上（详细见 §4）。

---

## 2. 已完成

### 2.1 核心玩法（对应文档全部章节）

| 模块 | 状态 | 说明 |
|---|---|---|
| 场景搭建 | ✅ | 由脚本按 `layout.json` 搭树，加桌子只改 JSON |
| 客人状态机 | ✅ | 7 态，与文档第四节一一对应 |
| 点单流程 | ✅ | 入座时自点 → 玩家接单才开票（票 = 「你答应了」的记录） |
| 共享订单 | ✅ | 订单挂桌子，内容 = 同桌所有座位 pending 拼接 |
| 后厨 | ✅ | 3s/道、队列、出餐口容量 1、阻塞式、气走不清队列 |
| 取餐 / 上菜 | ✅ | 两阶段检查；上菜只需走到桌边 |
| 用餐与结账 | ✅ | 各吃各的 12s，整桌一起起身，起身即结账 |
| 收拾 | ✅ | 2s，不可取消，进度条 |
| 气走 | ✅ | 整桌一起走、不结账、**不留脏桌** |
| 垃圾桶 | ✅ | 一次清空手上所有 |
| 收入 | ✅ | 只有菜钱，无小费/口碑/罚款 |
| UI | ✅ | 状态栏 + 订单栏 + 手上 + 两个弹窗 + 飘字 |
| 暂停 | ✅ | 弹窗期间 `get_tree().paused`，世界不推进 |

### 2.2 寻路与移动（多轮打磨的重点）

| 项 | 状态 | 关键点 |
|---|---|---|
| 网格 A* | ✅ | 4 方向、无斜线；**两份网格**（服务员 18px / 客人 8px 外扩） |
| 障碍来源 | ✅ | 每个碰撞形状**分别**作为障碍（桌子、左椅、右椅） |
| 直角化 | ✅ | 强制每段只能横或竖，斜段插直角拐角 |
| 客人进场寻路 | ✅ | 客人无碰撞体，斜线=硬穿，所以整条路径必须正交 |
| 碰到即触发 | ✅ | 上/下/左/右四边一致，不再依赖固定站位 |
| 落脚点换点 | ✅ | 够不到目标时**绕到够得着的一侧** |
| 卡住看门狗 | ✅ | 阈值 1.5px、2s 放弃、放弃时飘字说明 |
| 撞住自动改道 | ✅ | 本帧位移 < 期望 0.6 → 立刻重算路径（0.3s 冷却、独立预算、走顺回收） |
| 沿椅面滑行 | ✅ | 按真实碰撞法线偏转，最大 18°（×|sin 入射角|）；法线过期时不偏；正撞不拍横向分量 |
| 落脚点净空 | ✅ | `_clear_of_obstacles()`：按**真实碰撞盒**把落点推到 RADIUS+1=17px；点空地也走 |
| 不压桌面 | ✅ | 外扩 = 身体半径 + 2，实测最近离桌 45px |

### 2.3 按玩家反馈修掉的 bug（按时间顺序）

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 1 | 点空地撞桌子不绕开 | 点空地走的是另一条入口，完全不寻路 | 落脚点一律算出，寻路对所有指令生效 |
| 2 | 服务员体积擦桌时重叠 | 网格只外扩 6px | 外扩改成「身体半径 + 2」 |
| 3 | 靠近桌椅就出现斜线 | ①A* 拐点是格子中心，终点偏移几像素；②共线合并（4° 容差）把斜段并进竖段 | 加 `_orthogonalize()` 强制正交 |
| 4 | 订单栏挡住状态栏 | 位置写死 `y=176`，而状态栏占到 `y≈186` | 按状态栏**真实高度**推位置 + 分隔线 |
| 5 | 客人从桌子上走过去 | 左侧座位在桌子背面，直线必然穿桌 | 客人加寻路（客用 8px 网格）+ 整条路径正交化 |
| 6 | 气走后桌子变脏 | `leave_all(true)` 已设干净，走到门口又执行「全空就变脏」 | `member_vacated()` 先判「桌子是否仍 OCCUPIED」 |
| 7 | 只上 1 杯可乐就划红线 | 把「送了一部分」和「送完了」混为一谈 | 只有 `done` 才划线，部分送达改为减数量 |
| 8 | 票挡住最左边的桌子和客人 | 桌子 x=180 与订单栏重叠 | 桌子改成等间距居中（x=270/640/1010） |
| 9 | 钱一直是 0 | 票送齐时 `order` 被置空，而结账在**更晚**的「走到门口」时刻读 `order` → 直接 return | 金额单独存 `bill_amount`；并改为**起身即结账** |
| 10 | 客人踩到桌面 | 直角化的 `_blocked` 采样步长 8px 太粗，漏判「刚好擦过桌角」 | 步长改 4px；且只查桌子不查椅子 |
| 11 | 服务员压在客人身上 | 反复误判，实测**不重叠**（离客人 63~65px ≫ 31px 下限） | 未改逻辑，归因于缩放视图的视觉误读 |
| 12 | 站在椅子旁就能清洁 | ①判定区少了桌面最上 15px；②更关键：**「走到落脚点也算到达」**对需要触碰的指令也生效，而落脚点被椅子占住（离桌面 68px） | 判定区改成整个桌面；带触碰目标的指令**必须真碰到**；落脚点够不到时换到够得着的一侧 |

### 2.4 玩家确认过的设计决定

| 问题 | 决定 |
|---|---|
| 客人的菜从哪来 | 入座时自己随机点好（1~2 样，允许重复），接单只是揭晓 |
| 点几样菜 | 随机，1 样 50% / 2 样 50% |
| 一组来几人 | 1 人 70% / 2 人 30%；**整组必须同桌**，坐不下就在门口等 |
| 可乐怎么到手 | 饮料机 UI 选 → 进手上 → 走到客人身边上菜，与汉堡同一条路径 |
| 后厨按钮给谁做菜 | 玩家**自由预做**，与订单无关；多了就扔垃圾桶 |
| 大矩形既是后厨又是出餐口 | 大矩形里嵌小矩形：点小的取餐，点其余弹后厨 UI |
| 整组离场 | 各吃各的 12s，先吃完的留座等，整桌都吃完才一起起身 |
| 一人气走 | **整桌一起走** |
| 结账时机 | **客人离开座位（一起身）那一刻**（不是走到门口） |
| 手上能拿几份 | **2 份** |
| 上菜送哪份 | **自动匹配**：那桌缺哪样送哪样 |
| 一次送几份 | 该桌缺的、手上有的**都送完** |
| 手上不匹配的菜 | **继续拿着**，等下次点击送别桌 |
| 手满时点饮料机 | **不弹窗**，飘字「你拿不下其他东西了」 |
| 客人头顶显示 | **不显示菜名**（菜名只在左侧订单票上） |
| 订单票颜色块 | 加；并且**以后可以整体换成 sprite 图** |
| 清洁时长 | **2.0s**（文档写 3.0） |
| 柱子位置被椅子挡住时点桌子 | **绕到够得着的一侧**再清洁 |
| 桌子可碰判定区 | **整个桌面 80×60**（不是碰撞盒 80×45） |

### 2.5 扩展内容

| 项 | 状态 |
|---|---|
| 手上 2 份（`hand_capacity`） | ✅ 容量判断收敛到 `hand_has_room()` / `hand_is_full()` 单一入口 |
| 橙汁（`juice`，5 元） | ✅ 加进 `drink_items`，UI/订单/需求全自动 |
| 服务员手上两个方块 | ✅ **左肩 + 右肩对称** |
| 餐品贴图接口 | ✅ `ItemArt`：图片按 id 放进 `assets/items/` 即自动生效，找不到退回色块 |

### 2.6 工具与验证

| 工具 | 状态 |
|---|---|
| `check_scripts.gd` 编译体检 | ✅ 32/32 |
| `check_touch_zone` | ✅ 15/15（本轮新增「站位距离 ≤ 24px」断言，防锚点给多余额量） |
| `check_hud_target` | ✅ 18/18（本轮新增：顶部目标栏的位置/尺寸/不挡家具/星星点亮逻辑 + 左上角只剩一行） |
| `check_kitchen_ui` | ✅ 22/22（本轮新增：后厨弹窗方块布局、逐份队列、悬停 ✕ 删除） |
| `check_routes` | ✅ 7/7 |
| `check_walk_around` | ✅ 10/10 |
| `check_ortho` | ✅ 17/17（本轮修好取数 bug：原来 9/17 是假失败） |
| `check_no_jitter` | ✅ 7/7（本轮新增：连点桌子时不许高频来回） |
| `check_smooth_walk` | ✅ 15/15（本轮新增：撞椅子自己改道、不长时间停滞、行走不压进家具） |
| `check_angry` | ✅ 17/17 |
| `check_hand2` | ✅ 23/23 |
| `check_money` | ✅ 8/8 |
| `check_ticket` | ✅ 15/15 |
| `check_customer_walk` | ✅ 通过（客人不穿桌；需窗口模式跑） |
| `check_standing` / `check_counter` / `check_round3` | ✅ 之前轮次通过 |
| `diag_clean` | ✅ 4/4 |
| `DEBUG_WALK` 诊断开关 | ✅ 寻路 / 踩桌 / 换落脚点 / 清洁现场 四类日志 |

---

## 3. 进行中

**无。** 当前没有半成品改动；代码与文档描述一致，游戏零脚本错误启动。

---

## 4. 待办

### 4.1 技术债（我自己的工具，不影响游戏）

| 项 | 说明 | 建议 |
|---|---|---|
| ✅ 落脚点落在障碍里（本轮已修，**它就是抖动的主因**） | `_compute_goal()` 从「整团家具的外接矩形」外退 `RADIUS + 4`，而外接矩形比真实碰撞盒大（桌1 左椅把左边界拉到 x≈190，桌体碰撞盒左边在 x≈253）。结果从下方点桌子时落脚点算出来离碰撞盒只有 **12px < 身体半径 16** → 人**压进桌子 4.4px**。玩家看到的就是「碰到桌子在抖」（60Hz 左右摆 1.8px）。**点空地**的分支当时完全没做净空，更严重 | 已修：新增 `_clear_of_obstacles()`，在**真实碰撞盒**上迭代推到 `GOAL_CLEARANCE = RADIUS + 1`（17px，正好在可碰判定 17px 之内）；点空地分支也走同一处理。见 §6 本轮 |
| ✅ `check_ortho` 的 9/17 **不是走路回归，是取数 bug**（本轮修好，现 17/17） | 8 项失败都是「服务员停在起点附近、离落脚点几百像素」。真因有两条：① 路径是**懒构建**的（`_build_path` 在 `_walk` 里才跑），点击那一帧 `_waypoints` 还是空 → 脚本把「0 个拐点」当成「没有斜线」**假绿**；② `_settle()` 因为上一例残留的 `is_busy=false` 在**第一帧就返回**，量到的是上一例的位置 | 已修：点击后先 `await` 一帧；判据从「拐点序列」改成**逐帧监控真实移动方向**（横竖/斜帧分类，阈值 2px 排除 1~1.5px 的贴格过渡） |
| `tools/run_tests.gd` 部分分组过时 | 14 组中，第 6 / 10 / 11 / 12 组的断言停留在旧设计上（旧的桌子坐标、旧的「客人立刻变脏」时序、「一人气走另一位不受影响」）。**同一批行为已被 §2.6 的专门脚本覆盖且全部通过。** 本轮已做 A/B 对照（还原 `level.gd`/`ui_manager.gd` 跑同一套）确认这 10 项与开始界面改动无关。 | 删掉这几组，只留真正独有的纯逻辑测试（菜单价格 / 订单栏规则 / 后厨队列 / 桌子三态） |
| `run_tests.tscn` 第 7 组会访问违例崩进程（约 10 秒处） | 崩溃点是 `hud.gd` 那个无出口的自递归 `call_deferred`；本轮已给它加了重试上限与存活判断（见 §6 本轮）。**修完未再复现**，但整套 `run_tests` 本身已过时、且会串状态（§5.3），没有再投入排查。**正常启动游戏不受影响（玩家已实测）。** | 同上：清理这个套件时一并处理 |
| PowerShell 脚本里写中文会乱码 | `pwsh`（Windows PowerShell 5.1）按 ANSI 读 `.ps1`，带中文的脚本必须存成 **UTF-8 with BOM**，否则解析报 `The string is missing the terminator` | 写临时脚本时注意；或全用 ASCII |
| README 落后于实现 | 仍写着「手上只能拿 1 份」「收拾 3 秒」「桌子 y=380」「饮料机有出水口和杯子」「贴图接口在 waiter.gd 的 `carried_textures`」 | 按 `SPEC.md` 更新，或把 README 缩成「快速上手 + 指向 SPEC」 |
| `run_tests.gd` 的 122 项与 README 的「165 项」不一致 | 数字对不上 | 清理时一并订正 |

### 4.2 功能上可选做的事

| 项 | 说明 |
|---|---|
| 更清楚地表达「有单 / 已上菜」的状态 | 桌角小票标记**已按玩家要求删除**（一个空方块看不出是什么）。若以后需要，建议做成能一眼认出的样式（贴图或明确的图形） |
| 客人头顶「还差N」极简提示 | 菜名已移除；若多桌并发时觉得「这桌谁还在等」看不清，可以加一个不带菜名的数字提示 |
| 加第 4~5 张桌子 | 玩家庭间提过「中间多出来的位置可以以后放更多桌子」。当前是一排布局，加桌子需要另起一排或缩小桌距 |
| 真实美术 | 全部是代码画的占位图形。餐品已有贴图接口；家具 / 客人 / 服务员 / 地板仍是 `_draw()` |

### 4.3 文档

| 项 | 状态 |
|---|---|
| `SPEC.md` | ✅ 已按代码现状写完（20 节，含所有偏离标注） |
| `PROGRESS.md` | ✅ 本文件 |
| `README.md` | ⏳ 待按 SPEC 更新（见 §4.1） |

---

## 5. 已知问题

### 5.1 潜在代码问题（未复现，但按签名推断存在）

| 位置 | 问题 | 影响 | 优先级 |
|---|---|---|---|
| `game_state.gd` 的 `signal hand_changed(item_id: String)` | 类型标注是 `String`，但 `hand_take()` / `hand_remove()` / `clear_hand()` 调用的是 `hand_changed.emit(hand_name())`（也是 String，**这些没问题**）；而 `_ready()` / `reset()` 里同样是 String。**当前没有传 Array 的调用点**，所以只是标注与实际语义（现在是「整份手上的显示名」）略有偏差 | 无实际症状，但语义应正名为 `hand_changed(display_name: String)` | 低 |
| `run_tests.gd` 引用了已改名的方法 | 上一轮修 `_pts()` → `_seq_str()` 时改过一次；这类「方法改名后忘了同步调用点」在 GDScript 里**不会报警**，只在运行时炸 | 靠 `check_scripts.gd` 只能查编译错误，查不到「调用了不存在的方法」（那是运行时错误） | 中 —— **这是必须靠真实点击链路的测试来覆盖的一类风险** |

> **要强调的一条**：`check_scripts.gd` 只能证明「每个脚本单独能编译」。
> 它**证明不了**「A 脚本调用的 B 方法真的存在」—— 那是运行时才炸的错误。
> 所以新加可交互对象时，务必把点击路径加进用 `ClickRouter.handle_click()` 驱动的测试。

### 5.2 已知的视觉 / 观感问题（非 bug）

| 现象 | 现状 |
|---|---|
| 服务员可能和客人有一点视觉贴近 | 实测离客人 63~65px，远大于「不重叠」所需的 31px。归因于缩放视图下的视觉误读，**不修** |
| 椅子上的座位序号 1/2 | 一直画在游戏画面上（`table.gd` 的 `_draw_chair`），用于对着截图排查谁坐哪。用户确认「与本次任务无关，不动」 |
| 服务员站在桌子外侧时，身体和桌面矩形在垂直投影上有少量交叠 | 落脚点是从「整团家具外接范围」往外推的，物理上不碰家具；视觉上是「站在桌边」 |
| 客人走近座位时最后一小段可能与椅子重叠 | **有意为之**：客人本来就坐在椅子上，只有穿**桌子**才是 bug |
| 顶部区域（后厨 / 饮料机与桌子之间）留白偏大 | 已按玩家要求把两个物件贴墙（y=96），空档从 375px 收到约 219px；留白是为了以后加桌子 |

### 5.3 测试环境的坑（不是游戏 bug，但会误导排查）

| 坑 | 说明 |
|---|---|
| `check_ticket` 在 `--headless` 下**必超时** | 它用 `SubViewport` 做像素级验证，需要真实渲染上下文 → 必须用**窗口模式**跑 |
| `run_tests.gd` 不能 `--script` 跑 | 脚本当 main loop 时 autoload 还没创建，`Config` / `Game` 全报 Identifier not found → 用 `.tscn` 入口 |
| `_ready` 里 `add_child` 到 `root` 会失败 | "Parent node is busy setting up children" → 测试里先 `await get_tree().process_frame` |
| 测试脚本自身会串状态 | 写复现场景时，上一段结束时服务员可能还在 `_locked`，导致下一次点击被直接拒绝、**测出来的结论完全错**。必须先等空闲 + 换没被碰过的对象 |
| headless 下 `_draw` 只在进树时调一次 | 想靠「数 `_draw` 调用次数」做回归会永远不动；能用像素证明的事别用「应该会重绘」糊过去 |

---

## 6. 最近更新

按时间倒序（从早到晚为「最近的几轮」）。

### 本轮：后厨点单弹窗改版（方块槽 + 逐份队列）

> **玩家实测反馈（本轮内追加修复）**：先是「红 ❌ 一直在闪烁、鼠标输入有延迟」，
> 改成「常驻 + 只切 `shown`」之后仍然不行 —— **最后直接点不动、还卡死**。
> 根因是「悬停浮现」这套交互本身：❌ 恰好在鼠标下方，`mouse_entered/mouse_exited`
> 会在「出现 → 鼠标离开 → 隐藏 → 鼠标回来」之间每帧循环。
> **最终方案（玩家指定）：删掉悬停 ❌，改成对着队列里的菜品点鼠标右键删除。**
> 右键不需要任何额外控件、也不改变命中结果，从根本上避开了这个坑。
> 提示文案（玩家指定）：固定小字「左键点击餐品加入制作队列；右键点击队列中的菜品可将其删除」
> + 悬停 tooltip「右键可删除该餐品」。
>
> 教训写进 SPEC：**控件自己就在鼠标下方时，不要用显示/隐藏做悬停反馈。**

**玩家需求**：弹窗改成**便于日后放美术资源**的样式 —— 每道菜一个大方块图标、
下面一行「菜名 价格」，底部一行「队列：」显示本次已选、**可单独删掉某一份**。

**最终布局**（`scripts/ui/kitchen_popup.gd` 重写布局部分）：

```text
   后厨 · 点餐台
   ┌────┐ ┌────┐ ┌────┐        ← 96×96 方块（图标 + 右下角已点份数角标）
   └────┘ └────┘ └────┘
  汉堡 15元 薯条 6元 炸鸡块 12元
   队列： ■ ■ ■ ■ ■ …           ← 本次已点，按点击顺序，不限个数自动换行
      [完成]  [取消]
```

| 交互 | 行为 |
|---|---|
| **左键**点方块 | +1（**去掉了原来的 −/0/+ 按钮**） |
| **右键**点队列小方块 | 删掉**被点的那一份**（左键点它不做事，避免误触） |
| 完成 / 取消 | 行为不变（空队列点完成只提示、不关 UI） |

**两个关键实现决定**：

1. **队列存「逐份」而不是「按种类记数量」**：玩家要能删掉指定那一份，
   按种类记数量没法表达「删第 1 个汉堡还是第 2 个」。改成 `_queue: Array[String]`
   后 `build_queue()` 也变得直白（直接返回它）。
2. **方块不用 `Button.icon + expand_icon`**：实测它把图标塞进「文字之外剩下的高度」，
   96px 的按钮里图标只剩几像素、方块完全看不见（截图对比过）。
   改成「固定 96×96 容器 + `clip_contents` + 自绘 Panel 底 + 图标 + 角标」。

**换美术的路径**（玩家要的「方便日后放资源」）：图标走 `ItemArt.make_node`，
往 `assets/items/burger.png` 放一张图就自动生效，**面板、槽位、坐标都不用动**；
没图时退回 `menu.json` 的颜色占位块。

**新增体检 `tools/check_kitchen_ui.tscn`（22/22）**：方块尺寸、菜名价格一行、
点方块累加与顺序、**右键逐份删除**（删第 2 个只掉那一份）、**左键不误删**、
子节点不拦鼠标、tooltip 提示、空队列提示、取消清零。

**验证**：`check_kitchen_ui` 22/22、`check_kitchen_flow` 50/50、
`check_hud_target` 18/18、`check_level_mode` 160/160、`check_no_jitter` 7/7、
`check_touch_zone` 15/15；`run_tests` 206 通过 / 12 失败（历史遗留项，逐条不变）。
截图：`.artifacts/kitchen_ui3.png`。

### 本轮：左上角只留「第 N 关 · 倒计时 · 收入」

**玩家反馈**：把左上角那 7 行（钱 / 已接待人数 / 空桌数 / 待收拾数 / 在店人数 /
出餐口 / 后厨队列）**全删掉**，只保留「第 n 关、倒计时、收入」。

**改动**（`hud.gd` 一个文件）：

* 删掉那 7 个 `Label` 的构建与 `_set_stat()`；`apply()` 里不再写它们；
* 删掉那条状态栏分隔线（`_divider`）；
* 状态栏高度因此固定了 → **订单栏从「按状态栏真实高度推算」改成固定 `y=88`**，
  顺手删掉 `_layout_orders_below_stats()` / `MAX_LAYOUT_RETRIES` / `_layout_retries` /
  `_orders_placed` —— 那套「高度是 0 就 `call_deferred` 自己」的重试逻辑
  **曾经是个无限续期的崩溃源**（见旧注释），现在连根去掉了；
* 这些数值仍然在 `Game.hud_snapshot()` 里算着（工具与测试还在用 `in_store` 等），
  只是不再上屏 —— 所以没有任何逻辑被削弱。

**左上角现在的样子**：`第 1 关 · ⏱ 1:29 · 钱 45 元`（一行，剩 10 秒内转红）；
未开局时是 `第 1 关 · 准备中`。顶上正中仍是那三颗星。

**新增断言**（`check_hud_target` 13 → **18 项**）：七行数据确实不在 HUD 上、
「第 N 关…」那一行还在且带收入、订单栏固定在 y=88。

截图：`.artifacts/hud_clean.png`。

**验证**：`check_hud_target` 18/18、`check_level_mode` 160/160、
`check_kitchen_flow` 50/50、`check_touch_zone` 15/15、`check_smooth_walk` 15/15、
`check_ortho` 17/17、`check_money` 8/8、`check_no_jitter` 7/7、`check_hand2` 23/23；
`run_tests` 206 通过 / 12 失败（失败清单与改动前逐条一致）。

### 本轮：HUD 星级改成「顶部中间的三颗星」

**玩家反馈**：「玩家没办法在 UI 上看到每星目标」（原来只有一行纯文字
`★3 80 ／ ★2 70 ／ ★1 60`，藏在左上角，扫不到），要求做成「三颗空白星星、
星星下面标数字、达到了变黄」，并指定放在**顶部正中间**、放大、要醒目。

**做法**（玩家确认的三个选项：方案 2 + 去掉「还差多少」+ 1★→3★ + 星 40px）：

| 位置 | 内容 |
|---|---|
| **顶部正中**（新增 `TargetBar` 面板） | 三颗星（1★→3★ 从左到右，金额递增）+ 星下面 `当前/目标`（如 `45/60`）；达到哪一档，那颗星就变金黄实心 |
| **左上角**（回到状态栏第一行） | `第 N 关 · ⏱ 1:29 · 钱 45 元`（**去掉了「还差 N 元」**，玩家要求） |

* 复用了 `star_icon.gd`（结算界面在用的现成控件），没有新造零件；
* 整块 `mouse_filter = IGNORE`，不挡场景点击；
* 12px 圆角、42% 黑色底，和左上角状态栏的观感一致。

**布局踩的坑（值得记）**：

1. `PanelContainer` 里的 `Label` **默认会 wrap** —— 首次布局时标签拿到 1px 宽度，
   文字按字符**竖排**，高度瞬间涨到上百像素，把整块撑到 135px 高。
   修法：所有标签 `autowrap_mode = AUTOWRAP_OFF`。
2. 后厨窗口上沿在 **y=96**，这一块必须收在它上面；横排（星+数字）约 77px 正好，
   竖排（星 / 数字 / 信息行三行）会到 117px，直接压住「后厨 / 点餐台」标签。
3. 一度想把「第 N 关 · 倒计时 · 钱」也塞进目标栏，结果整块宽到 374px、
   右边界压住饮料机（x=735）。最后**把信息行还给左上角**，
   顶中只剩星星（176px 宽，右边界 728 < 735）。

**新增体检 `tools/check_hud_target.tscn`（13/13）**：水平居中、收在 y=96 以上、
不撞饮料机、不吃鼠标、三档按 1★→3★ 且递增、0 元全暗 / 到 1★ 只亮一颗 /
到 3★ 全亮、数字是「当前/目标」、左上角没有残留的旧 `★3 …` 文字行。

**顺手修好一个坏掉的工具**：`tools/screenshot.gd` 里 `for t in Game.tables:`
的循环体没缩进（注释还把第一行吃掉了），**它一直解析失败、截图功能是坏的**
（第 265 行报 `Expected indented block after "for" block`）。修好后出了下面这张图。

截图：`.artifacts/hud_target.png`（顶部正中三颗星 + `0/60 0/70 0/80`）。

**验证**：`check_hud_target` 13/13、`check_level_mode` 160/160、
`check_no_jitter` 7/7、`check_kitchen_flow` 50/50、`check_touch_zone` 15/15、
`check_smooth_walk` 15/15、`check_money` 8/8、`check_ortho` 17/17；
`run_tests` 206 通过 / 12 失败（历史遗留项，逐条不变）。

### 本轮：把关卡营业额门槛定稿（玩家指定）

| 关 | 时长 | 1★ | 2★ | 3★ | 3★ 强度 |
|---|---|---|---|---|---|
| L1 试营业 | 90s | **60**（40） | **70**（50） | **80**（60） | 0.89 |
| L2 午市 | 120s | **80**（60） | **90**（70） | **100**（80） | 0.83 |
| L3 晚高峰 | 120s | **90**（60） | **100**（80） | **110**（100） | 0.92 |
| L4 周末 | **130s**（120） | **100**（70） | **110**（90） | **120**（110） | 0.92 |
| L5 爆单日 | 150s | **120**（80） | **130**（100） | **140**（120） | 0.93 |

> 括号里是上一轮（临时下调版）的值，列出来是为了对照。

**只改数据**：`data/levels.json` 的 15 个金额 + **L4 的 `time_limit` 120 → 130**，
代码零改动。1 星统一 0.67 元/秒；三星 **0.83~0.93 元/秒**（L5 最紧 0.93），
玩家实测上限约 1.0 —— 从 L2 起三星基本压在实测上限的 83%~93%。
L4 放宽到 130 秒是为了让它的三星强度与 L3 同档（0.92），
否则「120 秒 / 120 元」会顶到 1.00。

**同步的地方**（避免文档和实际数值对不上）：

* `data/levels.json` 的 `_balance` 与每关 `note` 里的元/秒；
* `restaurant-v2-数值表.txt`（人看的参考表）；
* `SPEC.md` §12.1 过关线表、§12.2 星级表；
* `tools/check_level_mode.gd`：第 1 组**写死的时长与过关线期望**改成新值
  （90/120/120/**130**/150 与 60/80/90/100/120）。

**验证**：`check_level_mode` **160/160**（含 0~3 星边界逐档验：
第 1 关 60/70/80 … 第 5 关 120/130/140 都严格递增且边界正确，
L4 时长断言确认是 130 秒）。其它体检与 `run_tests`
（206 通过 / 12 失败历史项）均无变化。

### 本轮修：离饮料机/点餐台很远就能点 —— **真凶是 `command_move` 的赋值顺序**

> 上一轮我把这个 bug 判成「锚点给多了余量」并改了 `layout.json`。
> 锚点确实偏远（顺带修对了观感），但**不是主因**：玩家说「bug 还是在」，
> 而且截图里服务员还在门口、弹窗却是开着的。那说明是**提前触发**。

**用探针抓到的现场**（在 `_do_open` 里打一行日志就现形了）：

```
[DBG-DRINK] interact: 调 go_then，目标 (840,191)
[DBG-GOAL]  新指令 desc=去饮料机 goal→(310,489)   ← 算出来是**上一张桌子**的落脚点！
[DBG-ARRIVE] 判到达：pos=(310,485) goal=(310,489) dist=4.3 desc=去饮料机
[DBG-DRINK] _do_open 被调用！服务员在 (310.0, 485.0)   ← 人还在桌子那儿，弹窗已经开了
```

**根因**：`waiter.command_move()` 里 `new_goal = _compute_goal()` 被放在了
`_target` / `_touch_target` **赋值之前**。于是：

1. `_compute_goal()` 读到的是**上一条指令**的目标（那张脏桌）；
2. 新指令把「上一个落脚点」当成了自己的落脚点；
3. 服务员此刻离那个旧落脚点只有 4.3px（< `ARRIVE_EPS` 6），
   下一帧 `_walk()` 开头的到达判定立刻为真 → **原地就回调 `_do_open`** → 弹窗。

这解释了玩家的三句反馈：「离饮料机很远就能点」「点餐台也一样」
「我怀疑是交互被提前触发了」——三条都指向同一个原因。
只要在**服务员正忙**的时候点，就必然触发；空闲时点则正常（他本来就在附近）。

**修法（一行顺序）**：把 `_compute_goal()` 挪到字段赋值**之后**；
「保留路径」的判断拆成两步 —— 命中条件只看旧值（先存进 `prev_goal` / `prev_touch`），
赋值统一在后面。见 `command_move()` 里的说明。

**新增回归断言**（`check_kitchen_flow`，44 → **50 项**）：

```
PASS  饮料机：弹窗在第 191 帧才开（不是在原地第 0~1 帧就弹）
PASS  饮料机：弹窗打开时服务员离它 5px（≤24 = 真的走到了）
PASS  饮料机：他确实走过去了一趟（移动了 599px）
PASS  点餐台：弹窗在第 125 帧才开 / 离它 0px / 移动了 364px
```

> **【做了反向验证】** 把 `_compute_goal()` 挪回赋值之前（＝复现旧 bug），
> 这条断言立刻 2 项变红：弹窗打开时服务员离目标 **293~337px**。
> 而现有体检一直没抓到它，是因为它们直接调 `_do_open` / `_do_take` 处理器，
> **绕过了 `go_then` 的到达判定** —— 新断言走的是真实的 `handle_click` 链路。

### 本轮修：离饮料机/点餐台很远就能点（锚点余量）

**玩家反馈**：「离饮料机还有很远的距离就可以直接点饮料了，包括点餐台也有同样的问题」。

**排查结论：不是点击判定范围的问题，是「走到哪算到」的锚点太远。**

点一下 → 服务员必须走过去 → 走到锚点才弹窗，这条链路是通的（实测饮料机 2.5s、
后厨 1.6s 才弹）。问题是锚点本身太远：

| 目标 | 点击范围 | 服务员实际站位 | 离**视觉外框** |
|---|---|---|---|
| 饮料机 | 845..955 × 96..196（正确） | (840, **246**) | **50px** |
| 点餐台（后厨 UI） | 420..660 × 96..196（正确） | (540, **220**) | **24px** |
| 出餐口（取餐） | 同上小矩形 | (540, **231**) | 35px |

* `layout.json` 的锚点是「碰撞盒下边 + 44~58px」——余量太大；
* 到达走的是「走到落脚点」兜底判据（日志 `arrive → 因为「走到落脚点」算到达`），
  **没有提前触发**（实测差 0~4px 才结算），所以不是判定半径的问题；
* 这两个物件在自己的 `walk_to` 点上 `touches_from(..., 18)` 返回 **false**
  （桌子返回 true）—— 也就是说锚点连自己都够不着，本身就是「站在远处」的设计。

**为什么一直没被发现**：它不报错、功能全对，只是观感不对；
而既有体检（`check_touch_zone`）验的是「站在 walk_to 上够不够得着」，
`walk_to` 又是被测的那个值 —— 同源自证，量不出「太远」。

**修法（只改数据 + 加一条断言）**：

| # | 改动 | 值 |
|---|---|---|
| 1 | `layout.json`：后厨 `walk` 与 `pickup_walk` | `540,216` / `540,226` → **`540,188`** |
| 2 | `layout.json`：饮料机 `walk` | `840,240` → **`840,191`** |
| 3 | `check_touch_zone`：新增「站位距离」断言 | 服务员停在 `walk_to` 时离**视觉外框** ≤ **24px**（垃圾桶 32px 例外，未改它的手感） |

定值公式写进了 `layout.json` 的注释：
**`walk.y = 碰撞盒下边 + 身体半径(16) + 4px 视觉间隙`**。

**改完实测**（同一脚本前后对照）：

```
饮料机：站 (840,246)、离视觉外框 50px  →  站 (840,197)、离视觉外框 5px
点餐台：站 (540,220)、离视觉外框 24px  →  站 (540,190)、离视觉外框 0px
```

（最终站位是锚点与 `GOAL_CLEARANCE = 17px` 共同作用的结果：服务员停在碰撞盒外
26px，身体只差 10px，不重叠 —— 观感上就是贴着柜台站。）

**顺带修正一条旧断言**：`run_tests` 里「`walk_to` 必须在家具矩形外」用的是
`click_rect()`（= **视觉外框**）。后厨/饮料机的视觉框比碰撞盒高 28px，
所以「落在视觉框下沿、但完全在碰撞盒外」这种正确锚点会被它误判成 bug。
已改成拦真正的**碰撞盒**（那条断言的原本意图就是「别站进挡人的盒子里」）。

**本轮验证**：`check_touch_zone` 15/15、`check_kitchen_flow` 44/44、
`check_level_mode` 160/160、`check_no_jitter` 7/7、`check_smooth_walk` 15/15、
`check_ortho` 17/17、`check_walk_around` 10/10、`check_routes` 7/7；
`run_tests` 回到 **206 通过 / 12 失败**（历史遗留项，逐条与之前一致）。

### 本轮修：碰到桌子时抖动

**玩家反馈**：「偶尔玩家碰到桌子的时候会抖动」，要求排查。

**用探针量出来的真实现象**（不是靠猜）：

```
p=(344.8, 476.1)  v=( 12.4, 0)  slide=(0, 1)   ← 贴着桌子底边（法线朝下）
p=(343.6, 476.1)  v=(-74.8, 0)  slide=(0, 1)   ← 1.25px 后又被推回来
（9 帧一个完整循环，180 帧里 34 次真掉头；y 完全不动，只有 x 在 1.8px 内摆）
```

**根因：落脚点落在桌子的碰撞盒里。**

* 桌1 的碰撞盒是 `(270, 415, 80×45)`（底边 y=460），可碰判定 `touch_box` 底边也是 460、容差 `RADIUS + 1 = 17`。
* 但 `_compute_goal()` 是按「整团家具的**外接矩形**」退让的：从下方退 20px 得 y=480 ——
  那个点离**碰撞盒**其实只有 12px，身体（半径 16）**压进桌子 4.4px**。
* 于是每一帧都在「想走进落脚点 → `move_and_slide` 把人挤出来」，再叠加
  6.6 节沿面滑行给的横向分量，就变成 60Hz 的左右摆动。
* 「点空地」的分支当时**完全没做净空**（玩家点在桌沿上时最严重），
  实测就是上一版 `diag_jitter` 里那条 222 次来回的用例。

**修法（3 处，都在 `waiter.gd`）**：

| # | 修法 | 关键点 |
|---|---|---|
| 1 | 新增 `_clear_of_obstacles()`：在**真实碰撞盒**上迭代把落脚点推到 `GOAL_CLEARANCE = RADIUS + 1`（17px） | 17px 是刻意取的：可碰判定容差正好也是 `RADIUS + 1`，所以「站上去就够得着」，指令能正常结束 |
| 2 | **点空地**的落脚点也走同一净空 | 原来只处理「带触碰目标」的分支，点空地完全没有 |
| 3 | 完全正撞（退化）时滑行偏转**不再拍一个横向分量**，改成与行进方向同向的切向（偏转为 0） | 原来「挑一侧的垂线」正是横向摆动的来源 |

**顺带修掉两个放大器**（都是本轮为了定位而发现的）：

* `_next_step()` 的**吸附**从 6px 收紧到 1.5px：连点桌子时每次重算路径，
  新路径的首拐点常在身后，6px 吸附会把人**瞬移**回去（实测 `d=(5.33,-4.33)` 这种整格位移）。
* `command_move()` 里「点到同一目标/同一空地」时**保留现有路线**，不重算。
* 另外新增 `_drop_backward_first_step()`：身后且（一步能到 或 直线可达）的首拐点直接丢掉。

**新增回归体检 `tools/check_no_jitter.tscn`（7/7）**，量的是「方向来回帧数」：

```
[OK] 点脏桌1（从下方，连点）  来回 3 次（上限 6，5 次点击，最近离目标 5px）
[OK] 点空地贴桌沿（连点）    来回 0 次（上限 6，40 次点击，最近离目标 5px）
```

> **【做了反向验证】** 把 `GOAL_CLEARANCE` 临时改回 0（等于回到旧行为），
> 这个体检立刻 5 项变红、来回次数 **156~222 次**（正常是 0~4）。
> 也就是说它真的能抓到这次这个 bug，不是摆设。

**同时修好了 `check_ortho` 的 9/17**（见 §4.1）：那 8 项失败一直是**取数 bug**，
不是走路回归 —— 脚本在「路径还没懒构建」的那一帧取拐点（假绿），
而 `_settle()` 又因为上一例残留的 `is_busy=false` 第一帧就返回。
现在改成点击后先等一帧、并**逐帧监控真实移动方向**，17/17 通过
（实测全程横竖走，没有斜线路径）。

**这一轮所有相关体检**：`check_no_jitter` 7/7、`check_smooth_walk` 15/15、
`check_ortho` 17/17、`check_walk_around` 10/10、`check_routes` 7/7、
`check_standoff` 3/3、`check_touch_zone` 9/9、`check_level_mode` 160/160；
`run_tests` 仍是 **206 通过 / 12 失败**，与改动前逐条一致（历史遗留项）。

### 本轮修：撞到椅子就卡住、要再点一次才改道

**玩家反馈**：去桌子的路上**擦到椅子就卡住**，必须再点一次桌子，人才会迅速改道。
「这是个考验操作的游戏，所有操作都必须很顺畅很丝滑」。

**根因：自动改道的触发条件几乎永远不满足。**

`_check_stuck()` 判卡的标准是「这一帧位移 < 1.5px」。可是擦到椅子时
`move_and_slide()` 会**沿椅面滑**，一帧还能滑 3~4px —— 于是永远判不了卡，
路径也不会重算；等它真滑不动了，人已经贴死在椅角上。

而「再点一次为什么立刻就好」：新指令会用**当前位置**重算路径，
第二次的路线不擦椅子了。也就是说玩家手动做了游戏本该自己做的事。

**改法（两处）**：

| # | 改法 | 关键实现 |
|---|---|---|
| A | 把「没走出去」当一级信号：本帧真实位移 < 期望步长 × 0.6 → **立刻重算路径** | `_blocked_no_progress()`；重算有 `BLOCKED_REPATH_COOLDOWN` 0.3s 冷却 + 独立预算 6 次，走顺 1s 回收 1 次（否则撞几次就用光，又退回旧行为） |
| C | 撞上时按**真实碰撞法线**把速度偏到切向，沿椅面滑走，而不是顶着 | `_update_slide_dir()`（取所有碰撞法线之和，卡桌角时不来回抖）+ `_slide_steer()` |

**`_slide_steer` 踩了三个坑，都记下来**：

1. **退化情形被角度权重乘回 0**：正撞时 `cross` 恰好为 0，
   `w = MAX × |cross|` 把好不容易挑出来的一侧又乘成 0 → 返回原方向，
   等于顶着椅子不动。退化时改用满偏转。
2. **`slerp` 的 t 是「离起点多远」**：`slerp(0,-1 → -1,0, 0.75)` 只得 (-0.92,-0.38)，
   只转了 22.5°，与直觉相反 —— 用它写「偏 75%」会几乎不偏。
   改成 lerp 后**又发现**混合再归一化会悄悄改变夹角（叉积反算 23°，真实只有 7°），
   调参看到的是假数据。最终改成**显式 `rotated()` 定死最大偏转角 18°**，
   量多少就是多少。
3. **碰撞法线是「上一帧」的**：侧擦时它可能已经过期，按它偏转会把人往**回头**方向推。
   加一层筛选：只有前进方向与法线对抗（`dot < -0.2`）才算「还压着这个面」。

**新增体检 `tools/check_smooth_walk.tscn`（15 项全过）**，不看「最后有没有走到」
（那有 2s 看门狗兜底，旧代码也能过），而是量**有没有连续多帧「想走却没走出去」**：

```
[OK] 正撞：偏 18°，压向障碍 -1.00 → -0.95（沿面滑走）
[OK] 擦角：偏 0.0°（几乎不变，不会为了避让绕远路）
[OK] 真的发生碰撞：滑行状态 1 帧、同帧多碰撞最多 1 次，走了 211px
[OK] 贴椅子挤过去时最长停滞 0 帧（上限 30）
[OK] 6 条真实路线的行走最近离家具 ≥ 45px
```

> **【教训：这次调试里最花时间的两件事】**
> ① 体检脚本「点了空地」的目标落在**桌面**上，被桌子的 `interact()` 吃掉
> （空桌只飘字），量到的是「根本没走」，一度让我以为走路逻辑坏了 ——
> **测走路前先确认点击真的被受理了**。
> ② 脚本改完立刻跑，可能读到上一版编译产物（连着两次看到旧数值）。
> 现在改成改完文件停一下再跑，并用「运行时常量打印」确认版本。

**没动的地方**：`MAX_SLIDE_DEG = 18` / `BLOCKED_MOVE_RATIO = 0.6` / 冷却 0.3s
都是手感参数，全部写在 `waiter.gd` 顶部常量，改完存盘重启即可。
全套 `run_tests` 仍是 206 通过 / 12 失败，**与改动前逐条一致**（那 12 项是历史遗留：
饮品数量、出餐口容量等旧期望值）。

### 本轮修：换关时「手上图标不消失」+「脏桌延迟刷新」

**玩家反馈**（进第 4 关时）：① 小人手上还拿着第三关的餐品，图标没消失；
② 脏桌子延迟约 2 秒才刷新。

**两个 bug 是同一类错误：换关时只清了数据，没让视觉跟着走。**

| # | 根因 | 修法 |
|---|---|---|
| 1 | `_start_level_run()` 只调 `Game.reset_progress()` 清 `Game.hand`（数据）。而服务员肩上那两个方块画的是**他自己的 `carried` 数组**，由 `ClickRouter._sync_carried()` 同步 —— 那条线没被调用 | 改调 `router.clear_hand()`（它内部会 `_sync_carried()`） |
| 2 | 换关时直接写 `t.state = CLEAN_EMPTY`（绕过刷新）。桌子的**颜色和脏点全靠 `_refresh_look()` 里的 `queue_redraw()`**，所以视觉没跟上，要等鼠标动一下触发重绘 | 新增 `Table.reset_to_clean()` 作为唯一入口（改状态 + 刷新视觉成对），换关时调它 |

**关于「延迟 2 秒」**：那是"要等玩家鼠标动一下才触发重绘"的错觉，
不是真的等了 2 秒 —— 但也说明**不能靠鼠标事件去刷画面**（这个坑项目里踩过两次：
出餐口方块、脏桌，都是同一个病根）。

**新增断言**（`check_level_mode` 158 → **160 项**），并且**做了反向验证**：

```
PASS  夹具：服务员身上挂着一份餐（待会要被清掉）
PASS  **服务员身上没有残留的餐品标记**（视觉源，玩家报过这个 bug）
```

> **【这一条为什么之前漏掉了 —— 值得记】**
> 检查 6 里本来就有"残留检查"，但它只断言了 `Game.hand_items()`（数据源），
> 而 **bug 在 `waiter.carried`（视觉源）** —— 数据本来就是空的，所以 158 项全过却漏了这个 bug。
> 教训：**「清干净了没有」要同时查数据源和视觉源**；
> 只查数据，等于只验证了"代码以为自己做完了"。
>
> 反向验证：把两处修复还原后，这条断言立刻变红（`期望 0，实际 1`）—— 证明它不是装饰。

**回归**：`check_scripts` **41/41** ✅、`check_level_mode` **160/160** ✅、
`check_order_rule` 57/57 ✅、`check_kitchen_flow` 44/44 ✅、
`check_serve_hand` 32/32 ✅、`check_clean_seat` 33/33 ✅、
`check_start` 27/27 ✅，其余 6 个脚本全绿 ✅。

### 上一轮：加「星级」（每关 3 档门槛）+ 结算界面星星 + HUD 门槛提示

**玩家要求**：每关 3 种通关目标；结算界面在「通关成功」下面显示黄色星星
（3 星 = 3 颗黄；1 星 = 1 黄 + 2 白）。

**设计**（已逐条确认）：

| 问题 | 决定 |
|---|---|
| `target_money` 与新门槛的关系 | **它就是 1 星门槛**（= 过关线），另加 `star2_money` / `star3_money` |
| 0 星（未达标）时 | **不画星星**，只显示「通关失败」 |
| HUD 星级提示的位置 | 在「第 N 关 ⏱ 倒计时」那一行的**上方** |
| HUD 怎么表示门槛 | **数字**（`★3 105 ／ ★2 90 ／ ★1 75`），不画星星（一行三档会太宽） |
| 结算界面要不要另写"几星" | **不写**，只画星星 |
| 星星怎么画 | **代码画五角星**（`scripts/ui/star_icon.gd`），不用字符也不用贴图 |

**数值**（下表是**本轮定稿**的当前值；本文其余位置的同名表格是各轮历史快照）：

| 关 | 1★ | 2★ | 3★ | 3★ 强度 |
|---|---|---|---|---|
| 1 试营业 | 60 | 70 | 80 | 0.89 |
| 2 午市 | 80 | 90 | 100 | 0.83 |
| 3 晚高峰 | 90 | 100 | 110 | 0.92 |
| 4 周末 | 100 | 110 | 120 | 0.92 |
| 5 爆单日 | 120 | 130 | 140 | 0.93 |

> 1 星统一 0.67 元/秒；三星 0.83~0.93 元/秒，玩家实测上限约 1.0 ——
> 从 L2 起三星基本压在实测上限的 83%~93%，L5 最紧。

**改动**：

| 文件 | 改了什么 |
|---|---|
| `data/levels.json` | 每关加 `star2_money` / `star3_money` |
| `game_config.gd` | 读这两个字段 + **校验严格递增**（写反了会静默产生无意义判定，加载时纠正并记 `load_errors`）+ 新增 `star_money(关卡, 星)` 唯一入口 |
| `game_state.gd` | `stars()`（从高往低比门槛）+ `star_target(n)`；HUD 快照加 `star1/2/3` 与 `stars` |
| **`scripts/ui/star_icon.gd`** | **新增**：五角星多边形绘制（亮=金黄实心 / 暗=白色描边） |
| `result_panel.gd` | 标题下面加居中的 3 颗星；0 星整行隐藏 |
| `hud.gd` | 倒计时那一行上方加星级门槛行 |

**新增断言**（`check_level_mode` 从 94 → **158 项**）：

- **每个档位都验三个点**：门槛 -1（不给星）、门槛（给星）、门槛 +1（给星）
  —— 星级最容易错的就是"等于门槛算不算"和"差 1 元算几星"
- 5 关门槛**严格递增**（50<60<75 等）
- 不变式：**`passed()` 等价于 `stars() >= 1`**（在 5 关 × 14 个金额上全一致）
- 星星显示：达标时**只亮 1 颗**（且第 1 颗亮、后两颗暗）
- 未达标时**星星整行隐藏**

**回归**：`check_scripts` **41/41** ✅、`check_level_mode` **158/158** ✅、
`check_order_rule` 57/57 ✅、`check_kitchen_flow` 44/44 ✅、
`check_serve_hand` 32/32 ✅、`check_clean_seat` 33/33 ✅、
`check_start` 27/27 ✅，其余 6 个脚本全绿 ✅。

> **【测试顺序陷阱（本轮踩到）】** 「0 星时不画星星」这条断言，只有在它**之前**
> 已经跑过「达标并画了星星」才有意义 —— 否则"星星不可见"可能只是因为它从没被设过。
> 所以把「达标」用例挪到「未达标」之前，并在 `_ready` 里写了注释说明原因。

### 上一轮：调 L1/L2/L3 数值 + 新增饮品「柠檬水」（第 3 关解锁）

**玩家要求**：L1 = 120s/75；L2 = 150s/90；L3 = 150s/90 并新增一份饮料「柠檬水 2 元」。

**1. 关卡数值**

| 关 | 时限 | 目标 | 元/秒 |
|---|---|---|---|
| 1 试营业 | 120s | 75 | 0.63 |
| 2 午市 | **150s** | **90** | 0.60 |
| 3 晚高峰 | **150s** | **90** | 0.60 |
| 4 周末 | 120s | 110 | 0.92 |
| 5 爆单日 | 150s | 160 | 1.07 |

> L2 与 L3 数值相同是玩家确认的**有意设计**：L3 靠**新菜品**变难
> （柠檬水把人均单价拉低，同样 90 元要送更多趟），不是靠数值。

**2. 新增「菜单按关卡解锁」机制（这是本次的代码改动）**

原来菜单是**全局**的（`menu.json` 不区分关卡），没有"每关不同菜单"这回事。
按玩家要求做成**渐进解锁**：`menu.json` 的条目可以写 `start_level`
（默认 1），柠檬水写 `start_level: 3`。

**两处都必须尊重它**（漏一处就会出问题）：

| 位置 | 漏了会怎样 |
|---|---|
| 客人点单的抽取池（`Config.pool_at()`） | 前两关的客人会点柠檬水，而饮料机没有它的按钮 → **那份菜永远上不了，客人必气走** |
| 饮料机 UI（`Config.drink_items_at()`） | 第 1 关就能买到第 3 关的饮品 |

新增入口：`Config.item_start_level()` / `item_available_at()` /
`drink_items_at(level)` / `pool_at(level)`。

> **【饮料机菜单必须在"每次打开时"重建】** 弹窗节点跨关卡复用，
> 只在 `build()` 建一次的话，第 3 关弹出时还是第 1 关的菜单。
> 所以 `DrinkPopup.refresh_menu()` 由 `UIManager.open_drink_machine()` 在打开时调用。

**新增断言**（`check_order_rule` 第 9 节，57 项全过）：
- 第 1/2 关：点单池与饮料机菜单里**都没有**柠檬水
- 第 3/5 关：两处**都有**
- **大样本**：第 1/2 关各 1500 组订单，**一次都没点柠檬水**；第 3 关 1500 组里出现 558 次
- 加了柠檬水之后「每桌至少 1 份主食」仍然成立（2000 组 0 违规），且柠檬水不是主食

**回归**：`check_scripts` 40/40 ✅、`check_order_rule` 57/57 ✅，
其余脚本全绿 ✅。

> **【又踩一次"断言写死数量"】** `check_order_rule` 里写着「饮品 2 种」，
> 加柠檬水后立刻假失败。已改成 `>= 2` 并附带当前名字列表 ——
> 这类"把可增长的清单写死"的断言，本次已经是第三次踩。

### 上一轮：把后厨改回「一道一道做」（每道完整 3 秒）

**玩家反馈**：上一轮改成"不阻塞"之后，**一起下单的菜同时做好、同时出现**，
不符合现实逻辑。要求：下单的菜要**一道一道做**，制作时长仍是 3s。

**根因（我上一轮引入的）**：为了修「摆出第一份后白白多等一个周期」，
我把计时写成了 `timer = 0.0`。但 `update()` 第一件事就是判断 `timer <= 0` ——
**下一帧立刻又出一份**，于是 3 道菜在 3.0 / 3.05 / 3.1 秒挤出来。
我当时还把"3 份 6 秒"当成了正确结果（那其实就是这个 bug 的表现）。

**正确写法**：做完一份、若还有下一道，**计时重置为完整的 `_cook_time`**。
实测时间线（`check_kitchen_flow` 输出）：

```
[时间线] t=3.0s 做完第 1 道（pending 3 → 2）
[时间线] t=6.0s 做完第 2 道（pending 2 → 1）      ← 间隔正好 3.0 秒
```

**同时补掉一个真 bug**：`take_from_counter()` 原来**没有**在取走后补位
（只靠 `update()` 开头那一次）。表现是「把出餐口清空后，预存区那份永远上不了架」
（实测卡死：`queue` 空、`pending=1`、`timer` 冻在 3.0，第 2 道菜再也不出现）。
现在取走一份 → **当帧**补位，这正是玩家要的"空位一出现就瞬间出现"。

**回归**：`check_scripts` **40/40** ✅、`check_kitchen_flow` **44/44** ✅、
`check_level_mode` 94/94 ✅、`check_serve_hand` 32/32 ✅、
`check_clean_seat` 33/33 ✅、`check_order_rule` 40/40 ✅、
`check_start` 27/27 ✅，其余 6 个脚本全绿 ✅。

> **【验证纪律（本轮又被教训一次）】**
> 我为了「用什么量节奏」换了三种写法才写对，还写坏过一次打印格式
> （把字符串传给 `%.2f`，直接把断言函数打断）。
> 可靠的量法是**记录 `pending_count()` 每次下降的时刻**：
> 它每做完一道减 1，间隔就是"每道要多久"。
> **不要用「出餐口份数/内容变化」量** —— 玩家取餐会当帧补位，份数根本不下降。

### 上一轮：后厨改成「点了单就一直做」+ 出餐口容量 2

**玩家要求**：只要点单，后厨就开始制作，**而不是等取走一份再做下一份**。

**原设计（阻塞式）**：`update()` 第一句就是
`if _counter_full() or queue.is_empty(): return` —— 出餐口有餐时连计时都停，
玩家不回来取，第 2 道菜连开始做都不会。后厨被取餐动作牵着走。

**改后的分工**（两个住处，但总是一个真相）：

| 集合 | 含义 |
|---|---|
| `queue` | 还没开始做的 |
| `_cooked` | **已做好、等出餐口位置**的（预存） |
| 出餐口 | 玩家能立刻拿的，最多 `counter_capacity`（**1 → 2**）份 |

- 出餐口满了**不再停工**：做好的先进预存区；
- 玩家取走一份 → 空位**当帧**被预存补上（补位写在 `take_from_counter()` 里，
  不是等下一帧、更不是等再做 3 秒）；
- 只有预存区也堆满（= `counter_capacity` 份）才真的暂停做菜，防无限堆积；
- HUD「后厨队列」改用 `pending_count()` = `queue + _cooked`
  （只数 queue 会出现「队列 0」却还在冒菜）。

**修掉一个我在实现中引入的时序 bug**：原来写
`timer = _cook_time if not queue.is_empty() else 0.0` —— 摆出**第一份**时队列非空，
计时被重置成**满 3 秒**，于是 3 份菜要 **12 秒**而不是 9 秒（白白多等一个周期）。
正确语义是「一道做完、位置腾出来，下一道立刻开始」，所以 `timer = 0.0`。
> 这个 bug 是**测试抓出来的**：我用"推 9 秒后应该三份都做完"断言，结果第三份还在队列里，
> 一步步打印状态才发现计时被多算了一个周期。

**取餐规则**（玩家确认）：**一次把出餐口能拿的都拿走**，受手上剩余空位限制。

**新增 `tools/check_kitchen_flow.tscn`（31 项全过）**：
- 出餐口满了也把 3 份全做完（`queue` 空、1 份在预存）
- 取走一份 → **当帧**补满（不调用 `update` 就已经是 2 份）
- **容量 1 的老行为不破**（`Kitchen.new(3.0, 1)` 仍是"满了不覆盖、取走才补"）
- 走**真实点击链路**：手上为空时一次拿走 2 份；手上满了则拿不动

**回归**：`check_scripts` **40/40** ✅、`check_kitchen_flow` 31/31 ✅、
`check_level_mode` 94/94 ✅、`check_serve_hand` 32/32 ✅、
`check_clean_seat` 33/33 ✅、`check_order_rule` 40/40 ✅、
`check_start` 27/27 ✅，其余 7 个脚本全绿 ✅。

> **【一条纪律】** 测后厨**不要写"调几次 `update(x)` 就该怎样"**——
> 计时会在做完一份后重置，秒数很难心算。用 `fast_forward(N)` 推到确定的时间点，
> 或者直接断言**状态**（队列空没空、预存几份），别按秒数猜进度。本轮因此错了两版测试。

### 上一轮：调第 1 关数值 + 修「出餐口图标延迟消失」

**1. 第 1 关数值（玩家实测后重定）**

| 关 | 时限 | 目标 | 说明 |
|---|---|---|---|
| 1 试营业 | **120s** | **60 元** | 玩家实测 90 秒尽力只能赚 57 元 → 原定「90 秒 / 100 元」明显偏高 |

其余 4 关（120/180、120/240、150/320）**未改**。
> 我最初估算「2 分钟能赚 270~390 元」是错的：那个估算按「人均消费 7.8 元 ×
> 全部客人都会结账」算的，**没算上客人等不到会气走、以及来回跑腿的空档**。
> 教训：**难度目标必须用玩家实测数据定，不能用理论吞吐推算。**

**2. 修「出餐口图标延迟 1~2 秒才消失」**

**玩家反馈**：把餐从出餐口领走后，图标没立刻消失，要等 1~2 秒（动一下鼠标才更新）。

**根因（关卡模式引入的）**：出餐口的重绘靠**订阅 `Kitchen.counter_changed`**
（`kitchen_window.gd`）。而 `reset_progress()` 会 **`new` 一个新的 `Kitchen`** ——
出餐口还挂在**旧对象**上，新对象发信号没人听，于是不重绘；
一动鼠标、悬停状态变化触发 `queue_redraw()`，图标才消失。

**修法**：`Game` 新增 `kitchen_changed` 信号（换 Kitchen 时广播），
出餐口订阅它并**重绑**到当前 Kitchen 上。

**新增回归断言**（`check_level_mode` 第 9 节）：直接检查
「当前 Kitchen 的 `counter_changed` 上挂着出餐口的回调」，覆盖
「开局后」「出餐后」「取走后」「再换一局后」四个时点。
**并做反向验证**：还原成旧行为后这 4 条全红（`取走后订阅仍在` 失败）。

> **【关于「是不是它导致出餐变慢」】不是。** 这段延迟是**纯视觉**的：
> 餐在 `_do_take()` 里是**同步**进入手上的（`Game.kitchen.take_from_counter()`
> → `take_into_hand()`），玩法层面没有变慢，后厨该什么时候出餐还是什么时候。
> 玩家感觉「慢了」是因为看不到手上/出餐口的变化，会重复点击或等待。

**回归**：`check_scripts` 39/39 ✅、`check_level_mode` **94/94** ✅、
`check_start` 27/27 ✅，其余 10 个脚本全绿 ✅。

### 上一轮：加「关卡模式」（一局一局）+ 结算界面

**需求**：无限模式玩家没有正反馈、容易疲劳 → 改成一关一局；固定时长 + 固定营收目标。

**已与用户确认的设计**（详见 SPEC §12.1）：固定时长到点结算；只看营业额（气走=少赚，
不额外扣钱）；**每关营收独立**（进关归零）；达标「下一关」、未达标「重试本关」；
最后一关过了「重玩第 1 关」；**不存盘**；**第一版不做星级**；
结算界面只显示「通关成功/失败 + 营业额 + 接待客人总数」；
**倒计时在弹窗期间照走**。

**数据**：新增 `data/levels.json`（5 关，加关卡只改 JSON，含逐字段校验 + 兜底）。

**改动**：

| 文件 | 改了什么 |
|---|---|
| `data/levels.json` | 新增。L1 90s/100 → L5 150s/320 |
| `game_config.gd` | 读关卡表 + 校验（非法条目丢弃并记 `load_errors`）+ `level_at()` 越界夹取 |
| `game_state.gd` | 关卡状态（`level_index` / `time_left` / `run_active`）、`start_level()` / `tick()` / `passed()` / `has_next_level()`、`level_finished` 信号；**拆出 `reset_progress()`**（见下） |
| `ui_manager.gd` | 推进倒计时、`level_finished` → 弹结算、结算界面 API、`run_requested` 信号 |
| `result_panel.gd` | 新增。结算界面（大标题 + 两行数据 + 按钮） |
| `level.gd` | `_start_level_run()`（重开一局的唯一入口）、订阅 `run_requested` |
| `hud.gd` | 状态栏第一行：第 N 关 · 倒计时（<10s 变红）· 还差 N 元 |
| `waiter.gd` | **修既有 bug**：`cancel_command()` 没解除「正在收拾」登记 → 那张桌永久不可坐 |
| `table_rules.gd` | 新增 `clear_all_cleaning()`（开局兜底清登记表） |

**本轮踩到并修掉的三个坑（都不报错，很能骗人）**：

1. **倒计时不能放在 `Level._process`**：弹窗会把整棵树 `paused = true`，
   而引擎**不给暂停树里的节点发 `_process`** —— 于是「弹窗期间倒计时照走」
   这条要求根本没生效。改由 `UIManager._process`（`PROCESS_MODE_ALWAYS`）推进。
2. **`level_finished` 信号没人接**：结算界面做成了「等人调 `show_result()`」，
   但没有任何地方把那根线接上 → 时间到了什么都不发生。
   更阴的是结算面板在 `build()` 里已经有默认标题文字，
   于是断言「标题=通关成功」还会通过，看起来像「只有两行数据是空的」。
3. **`Game.reset()` 会清空 `tables` 注册表**：而桌子只在 `Level._ready()` 里
   注册一次 → 「点开始游戏」之后 `Game.tables` 一直为空、**客人再也不生成**
   （实测：进游戏 10 秒 0 位客人）。拆成 `reset()` / `reset_progress()` 两个函数。

**测试**：新增 `tools/check_level_mode.tscn`（**86 项全过**），
特意钉住了两件容易被后人"顺手改坏"的事：
- **弹窗期间倒计时照走**（不能因为"弹窗时世界暂停"就顺手改成暂停）；
- **换关不残留**（客人/订单/脏桌/收拾登记/服务员锁全部要清干净）。

**回归**：`check_scripts` **39/39** ✅、`check_level_mode` 86/86 ✅、
`check_start` 27/27 ✅（**顺带修了它自己的一个坑**：它建两个 Level，
而 `run_requested` 是全局信号 → 已释放的那个也收到通知、把 `Game.tables` 清空；
现在只建一个）、其余 9 个脚本全绿 ✅。

**写测试时又踩的坑**：结算界面会 `paused = true`，下一个用例不清这个状态
就直接开始测试 → 世界仍暂停、倒计时不动、时间永远到不了，
**16 条失败里有一半是这一个原因造成的假失败**。现在每个用例开头都先 `_reset_to_idle()`。

### 上一轮：开始界面副标题改文案

**玩家要求**：「成为麦麦快餐店的一名服务员，享受牛马人生吧！」
→ **「成为麦麦快餐店的服务员，燃烧你的牛马之魂吧！」**

**只改了一处**：`Constants.START_SUBTITLE`。
因为开始界面（`start_screen.gd`）与 `check_start.gd` 的断言都**引用这个常量**
而不是各抄一遍字面量，所以不存在「界面改了、断言还在比对旧文案」这种不一致
—— 这正是把文案集中到 `constants.gd` 的意义。另外同步更新了 `SPEC.md` §14.6。

**验证**：`check_start` **31/31 通过**（副标题断言已变成新文案），
截图 `.artifacts/start_screen.png` 已重新生成并肉眼确认排版正常
（20 个字仍然只占屏宽约三分之一，不会挤压标题和按钮）。

### 上一轮修：点空桌的提示语 + 文案改名

**玩家反馈**（两件事）：

1. 手里拿着菜去点**空桌子**，弹出的是「这不是他要的菜」；
   应该提示「桌子是空的」才对。
2. 「这不是他要的菜」这句文案改成「**这桌没点这个菜**」。

**根因（第 1 条）**：`Table.interact()` 的**判断顺序**错了 ——
「手上有菜 → 没人要 → 说没点这个菜」曾经排在「桌上有没有人」**前面**，
所以空桌 + 手上有菜永远走不到「这桌是空的」那条分支。

**修法**：把「桌上一个客人都没有」这一条**提到「手上有菜」之前** ——
「桌上有没有人」比「手上是什么菜」更基础。改完的分支顺序：

```
1 脏桌 → 去收拾
2 有客人在等点单 → 等价于点客人
3 桌上没人 → 「这桌是空的」        ← 提到这里
4 有客人 + 手上有他要的 → 去上菜
5 有客人 + 手上有菜但没人要 → 「这桌没点这个菜」
6 有客人 + 空手 → 「手上没有餐品」
```

**修法（第 2 条）**：`Constants.MSG_WRONG_DISH` 改成「这桌没点这个菜」；
顺带把原来硬编码在 `table.gd` 里的「这桌是空的」也收进常量
（`Constants.MSG_TABLE_EMPTY`），符合项目「界面文案集中在 constants.gd」的规矩。
注释里写清楚了这两句话**问的是不同问题**，不能混用：
前者=桌上没人，后者=有人但不要这个。

**测试（`check_serve_hand` 扩到 32 项，全过）**：

| 用例 | 期望提示 |
|---|---|
| 手上有菜 + 点空桌 | 「这桌是空的」← 玩家报的场景 |
| 空手 + 点空桌 | 「这桌是空的」 |
| 有客人但没点这个菜 | 「这桌没点这个菜」 |
| 空手 + 点有客人的桌 | 「手上没有餐品」 |
| 常量断言 | 旧文案「这不是他要的菜」已不存在 |

**回归**：`check_scripts` **37/37** ✅、`check_serve_hand` 32/32 ✅、
`check_clean_seat` 33/33 ✅、`check_order_rule` 40/40 ✅、`check_start` 31/31 ✅，
其余 7 个脚本全绿 ✅。

**又踩了一次自己写测试的坑**：验证「空手点空桌」时忘了先把手上的菜清掉，
测出来的其实是「有客人 + 手上没有他要的菜」那条分支 —— 假失败。
（同一类错误本轮已经犯过三次：**夹具没清干净就断言**。）

### 上一轮修：手上两份汉堡送一桌，两份都没了

**玩家反馈**：手拿两份汉堡，点一桌只点了一份汉堡的客人 → **两份汉堡都消失了**，
按理说只该少一份。

**根因**（一行返回值没看）：

```gdscript
for item in items:
    table.call("assign_delivery", item)   # ← 返回值被丢掉了
    router.remove_from_hand(item)         # ← 无条件扣一份
```

`Customer._wanted_in_hand()` 是**按手上的份数**列出来的，会带重复：
手上两份汉堡、这桌只缺一份时它返回 `["burger", "burger"]`。
循环第一遍真的送出并上榜；第二遍 `assign_delivery` 已经找不到想要的客人
（**返回 null**），但代码不看返回值，照样扣一份 → **两份全没了**。

**修法**：把「实际送出去几份」当成唯一真相。
`assign_delivery()` 的返回值就是它（返回被服务的那位客人，没人要则 null）。
新增 `Customer._deliver_items()`（数出每样送成功几份）与
`_apply_delivered()`（按份数扣手上的菜，顺带飘字「上菜 汉堡×1」）。

**新增 `tools/check_serve_hand.tscn`（20 项全过）**：

| 用例 | 断言 |
|---|---|
| 手上 2 份汉堡，桌上只要 1 份 | **上完菜手上还剩 1 份**（原来会变成 0） |
| 桌上点 2 份、手上有 2 份 | 两份都送出去、手上空、客人拿到 2 份 |
| 手上是不匹配的菜 | 手上不动、不误扣 |
| 手上橙汁+汉堡、这桌只要橙汁 | 只送走橙汁，**汉堡继续拿着** |

**并且做了反向验证**：把修复还原成旧写法（无条件扣手上）后跑同一条测试 →
`FAIL 上完菜手上还剩 1 份汉堡（期望 1，实际 0）`。
**证明这条测试真的能抓到这个 bug，不是装饰。**

**回归**：`check_scripts` **36/36** ✅，`check_serve_hand` 20/20 ✅，
`check_clean_seat` 33/33 ✅，`check_order_rule` 40/40 ✅，
`check_customer_walk` / `check_angry` / `check_hand2` / `check_money` /
`check_touch_zone` / `check_routes` / `check_walk_around` 全绿 ✅。

### 上一轮修：玩家实测报的「收拾桌子时新客人直接落座」

**玩家反馈**（附带截图）：点桌子去清洁，清洁过程中新客人直接落座；
清洁完成后又有新客人在同一个座位坐下，两个客人重叠；
新客人走后旧客人的图标还留在椅子上，但点桌子弹出「这桌是空的」。

**根因**：收拾进度只存在**服务员**身上（`waiter.cleaning_table`），桌子自己
一直是 `DIRTY`；而**选座发生在更早的时刻** —— 玩家点下去那一瞬间桌子还是
`CLEAN_EMPTY`。两者对「这张桌现在能不能坐人」没有共识：

```
点脏桌 → 服务员走过去（这几帧里桌子已不是 DIRTY）→ 客人直接落座
→ 收拾完成 finish_clean() 无条件把桌子设回 CLEAN_EMPTY
→ 椅子上坐着人，桌子却宣称「干净又没人」→ 点它就说「这桌是空的」
```

**修法（三处，收敛到一个判断入口）**：

1. `TableRules` 新增 `is_seatable(table)`：排除「正在收拾」+ 要求 `CLEAN_EMPTY`。
   **选座（`SeatManager` 三处）、HUD 空桌数（`Game.available_tables()`）、
   全店空位统计**全部改走它，不再各自判断 `state`。
2. 「正在收拾」登记在 `TableRules`（跨模块可见）：`begin_clean()` 登记、
   `_end_clean()` 解除 —— **严格成对**，中断路径也会解除。
   > 解除若漏掉，那张桌会**永久不可坐**，比原 bug 更难查。
3. `Table.finish_clean()` 加守卫：**有人在座就保留 `OCCUPIED`**，绝不洗成空桌。

**新增 `tools/check_clean_seat.tscn`（33 项全过）**，其中：

- `收拾期间 121 次选座，一次都没挑到这张正在收拾的桌` ✅
- `收拾期间它始终不可坐（滚动记录 121 帧）` ✅
- `finish_clean 之后桌子仍是「有人」` ✅
- `收拾完的桌子可以重新坐人（没有永久标记）` ✅ ← 防止修 2 引入新 bug

**回归**：`check_scripts` **36/36** ✅、`check_start` 31/31 ✅、
`check_order_rule` 40/40 ✅、`check_clean_seat` 33/33 ✅、
`check_customer_walk` / `check_angry` / `check_hand2` / `check_money` /
`check_touch_zone` / `check_routes` / `check_walk_around` 全绿 ✅。

**写这个测试时踩的坑（值得记）**：

- **不要用「瞬移客人 + 手动调 `_on_customer_seated`」来伪造现场**：
  那位客人身上的拐点还指着门口，它下一帧就沿旧路径走回门口、归还座位，
  把桌子改回 DIRTY。断言于是全错，看起来像产品 bug，其实是**我的伪现场**。
  最终改成在**选座入口**上做确定性验证（收拾期间反复 `pick_group` 必须挑不到）。
- **不要「等循环结束再断言窗口期内的状态」**：收拾结束那一帧
  `is_seatable` 会立刻翻回 true。要在循环里**滚动记录**「有没有出现过 true」。
- **不要依赖「上一步留下的数据」**：各小节会互相清理，依赖它会得到假失败。

### 上一轮修：玩家实测报的「第一组客人之后不再来客人」

**玩家反馈**：第一组客人生成后，整局再也不生成客人。

**根因（我引入的）**：`customer_manager.gd` 里有一个 `_group_size` 计数器表达
「上一组还没走完」，它只在 `spawn_group()` 末尾被赋值，**没有任何地方清回 0** ——
旧代码是靠「`_group_seats` 排空时顺手归零」复位的，我改成同帧生成时把那个复位点一起删了。
于是第二行守卫 `if _group_size > 0: return` 永远成立，**整局只来一组客人**。
不报错、不警告，玩一两分钟才看得出来。

**修法**：不再用「需要手动复位」的计数器，改成直接查场上客人的状态
（`any_walking_in()`）—— 这个状态由客人自己维护、随它落座自动消失，
不存在「谁忘了复位」。`_group_size` 字段整个删除。

**顺带修掉同一轮引入的第二个 bug**：改代码时把 `Door.end_group()` 的缩进碰掉了，
它落到了 `for` 循环**里面**，等于每生成一位就把组内序号复位 →
**两位客人的门口起点重叠（错开量实测 0px）**，正是用户要求避免的。
现在移回循环外（`begin/end` 必须是「一组一次」的配对）。

**再顺带修掉一个更严重的坑（上一轮埋的）**：
`scenes/main.tscn` 里写过 `show_start_screen = true`，于是**任何**加载 `main.tscn`
的脚本（含全部 18 个工具）都会 `UI.begin(true)` → **整棵树被暂停**。
引擎不给暂停树里的节点发 `_process`，所以靠 `_process` 跑逻辑的测试会被
**静默冻死**（实测：客人生成器一次都没跑，而工具照样打印「全部通过」）。
现改为：`main.tscn` 保持安全默认（不写这个字段），入口判据在 `_ready` 里
同步算 → `父节点是 root` **且** `current_scene == self`。
**实测确认**引擎在 `main.tscn` 启动时 `_ready` 内读到的 `current_scene` 就是 Level
（上一轮我得出「引擎在 _ready 之后才赋值」的结论是**错的**，被自己手工设
`current_scene` 的探针误导了）。

**新增回归断言**（`tools/check_order_rule.gd` 第 8 节）：真的开着自动生成、
手动推进 30 秒，断言「一共坐下过 ≥3 位客人」。实测 **5 位**。
> 这条断言的价值在于：客人到门口就 `queue_free`，「当前有几个客人」随时回到 0，
> 所以「整局只来一组」这种死锁**从当前数量上看不出来**。
> 因此 `Level` 新增 `seated_total` 累计字段专门供它断言。

**过程中踩到的工具坑（值得记）**：
- **`int(null)` 不返回 0，而是抛 `Nonexistent 'int' constructor`**。
  客人容器为空时 `in_store_count()` 返回 null，`int(spawner.call(...))` 直接报错。
  这种脚本错误会**中断当前协程**：后面的断言一行都不跑，而工具照样把已有断言
  打成「全部通过」——**看上去一片绿**。本轮被它骗过两次
  （第一次：第 8 节断言根本没执行却显示 37/37）。
  已加 `_num()` 安全取值函数替代所有 `int(动态返回值)`。
- 因此**「只看绿不看数字」是不够的**：要确认断言真的执行了（看输出里有没有那一行）。

**回归**：`check_scripts` 35/35 ✅、`check_order_rule` **40/40** ✅、
`check_customer_walk` 12/12 ✅、`check_angry` 17/17 ✅、
`check_hand2` 23/23 ✅、`check_money` 8/8 ✅。

### 本轮：加「炸鸡块」+ 客人点单必须点主食

**需求**：① 加一道主食「炸鸡块」；② 每桌客人至少点 1 份主食，不能只点饮料。

| 项 | 结果 |
|---|---|
| 炸鸡块 | ✅ 12 元、占位色 `b5651d`、算主食；后厨 UI 自动多一个按钮 |
| 点单规则 | ✅ 每**桌**至少 `min_main_per_group`（默认 1）份主食；不满足 → **整组重摇** |
| 主食认定 | ✅ `menu.json` 里带 `"main": true` 的后厨菜（汉堡 / 薯条 / 炸鸡块）；饮品永远不是主食 |
| 算「整桌合计」 | ✅ 允许「一人汉堡+可乐、另一人只喝可乐」；两人都只点饮料不允许 |
| **同组客人同一帧生成** | ✅ 原先是「前一位落座后下一位才生成」；现在一次建完整组，门口 x 轴错开 36px |
| 订单定好的时机 | ✅ **生成那一刻**整组一起摇好（入座 / 接单只是揭晓） |
| 新工具 | `tools/check_order_rule.tscn` —— **35/35 通过**（含 8000 次大样本：违规 0） |
| `check_scripts` | **35/35** ✅ |

**本轮踩到并记下来的坑**：

1. **「按桌校验」和「各自摇单」是有冲突的**：现有代码每位客人各摇各的。
   若各自摇、事后补摇某一位，「谁被迫点主食」就永远落在同一个人身上
   （总是后入座那位），客人之间机会不平等。**所以必须整组一起摇、整组重来。**
2. **改成同组同帧生成后，门口的错开是必须的**：客人不是物理物体，
   都从同一个点出发会让两个人的圆完全重叠着走进店，看着像一个人。
   错开用 `Door.begin_group/next_group_spawn/end_group` 管（一组一复位）。
3. **改起点不能复用 `Customer.setup()`**：它还负责 `reached_seat` / `gone`
   两个信号连接 —— 已经连过再走一遍，两个回调会**各触发两次**
   （座位记两次、订单栏刷两次），而且**这种重复连接不报任何错**。
   所以另开了 `Customer.place_at()`：只挪位置 + 重算进场路径。
4. **`menu.json` 缺 `main` 字段时按「是主食」处理**（`Config.MAIN_BY_DEFAULT`）：
   旧菜单文件不会因为漏一个字段就让整条规则**静默失效**；
   要排除某道菜必须显式写 `"main": false`。
5. **无解情况必须有出口**：菜单里一道主食都没有时不能一直重摇（死循环），
   要直接返回普通随机结果；摇满上限仍未满足时才「换掉单里最后一样」兜底。
   两条都有断言覆盖。

**回归结果**：

- `check_scripts` 35/35 ✅；`check_order_rule` 35/35 ✅；
- `check_customer_walk`（客人不穿桌）12/12 ✅ —— 同组同帧生成没有破坏进场寻路；
- `run_tests.tscn` 里那两条菜单数量断言已按用户要求改对
  （后厨 2→3 道、饮品 1→2 种）；
- **`check_ortho` 9/17（8 项「服务员没走到落脚点」）—— 已 A/B 证实是改动前就存在的**：
  把客人相关改动全部还原后跑同一套，**8 项失败与数值完全一致**
  （`停在 (1150,600)，离落脚点 1297.1px` …）。它测的是**服务员**走路，
  与本次改动无关，**本次不修**（已记入 §5.1）。

### 上一轮：加「开始界面」（标题画面）

**需求**：游戏开头加一个开始界面，**点了「开始游戏」才进入游戏**。

| 项 | 结果 |
|---|---|
| 新增 `scripts/ui/start_screen.gd` | 标题「麦麦快餐店」56px 金黄 / 副标题 / 大按钮 240×64，三块整体居中 |
| 背景 | **纯黑不透明**遮罩（用户确认「一点餐厅都不露」）+ HUD 隐藏 |
| 世界 | 冻结（`get_tree().paused`）——客人不生成、后厨不计时 |
| 进入游戏的时机 | 点按钮 → 收起界面、恢复 HUD、解冻、**空桌计时归零** |
| 「出不出开始界面」 | 入口判据在 `level.gd` 的 `_ready` 里同步算：`父节点是 root` **且** `current_scene == self`（**不要**在 main.tscn 里写 `show_start_screen = true`，那会让所有工具都弹界面并冻结世界，见本轮修的第二条） |
| 新工具 | `tools/check_start.tscn` —— **31/31 通过**，产出 `.artifacts/start_screen.png` / `after_start.png` |
| `check_scripts` | **32/32 → 34/34**（新增 `start_screen.gd`、`check_start.gd`） |
| HUD | 删掉左上角「餐厅物语」标题行（游戏名只在标题画面出现），订单栏自动上移 |

**本轮踩到并记下来的坑（都写进 SPEC.md §14.6 了）**：

1. **「自动判断主场景」三种写法全部不成立**：
   ① 父节点是不是 root —— `run_tests.gd` 等就是 `root.add_child(level)`，会误判，
   整个测试套件被冻在标题画面上（协程不推进，**静默挂死**）；
   ② `current_scene == self` —— **引擎是在 `_ready` 跑完之后才赋值 `current_scene` 的**
   （实测 `_ready` 同步 / 同帧 deferred / 下一帧三个时点读到的都是别的节点），
   在 `_ready` 里永远为假 → 真实启动反而不出开始界面；
   ③ 延后一帧再判断 —— 能对，但依赖时序巧合，测试里前一个 Level 还在树上时会漏判。
   **最终方案：让入口自己声明**（`main.tscn` 设标志位），18 个工具一行都不用改。
2. **只靠半透明遮罩盖不住 HUD**：第一版遮罩 82%，截图里左上角
   「钱：0 元 / 已接待人数：0 …」和左下角「手上：空手」全都露着。
   现在改成「遮罩压暗世界 + **显式隐藏 HUD**」两件事分开做。
3. **隐藏 HUD 的出口有两个，必须都恢复**：`begin(false)` 和 `begin_game()`
   各有一处，漏一处就会出现「进游戏后没有 HUD」这种**不报错**的故障（已修）。
4. **暂停期间的按钮必须 `PROCESS_MODE_ALWAYS`**，否则「开始游戏」按不动，永远卡在标题画面。
5. **空桌计时是墙上时钟**：`empty_table_delay` 用 `Time.get_ticks_msec()` 差值，
   场景树暂停它照走 → 必须「点开始那一刻归零」，否则客人秒到。

**回归结果**：

- `check_scripts` 34/34 ✅；`check_start` 31/31 ✅；
- `run_tests.tscn` 的 10 个失败项 **A/B 对照证实是先天的**：把
  `level.gd` / `ui_manager.gd` 还原成改动前，跑出**完全相同的 10 项失败**
  （208 通过 / 10 失败），正是 §4.1 记的那批过时断言，与本次改动无关。
- **玩家实机试玩确认游戏无 bug、无崩溃**（正常启动路径与测试套件是两条路）。

**顺带修掉一个潜伏的崩溃（`hud.gd` 的自递归延迟调用）**：

`_layout_orders_below_stats()` 原来是「高度读到 0 → 无条件 `call_deferred` 自己」，
**没有出口**。只要容器高度一直读成 0，延迟调用就一帧接一帧永远排下去，
最后在节点释放之后继续跑 → 进程级访问违例（`0xC0000005`），
崩溃回溯恰好停在那句 `call_deferred`（`tools/run_tests.tscn` 第 7 组，约 10 秒处）。

- **为什么会读到 0**：`UI.hud` 是 autoload 单例，而 `build()` 每被调一次就往
  **同一个 HUD 上再叠一套控件**（旧的既不释放也不隐藏）。测试里 9 个分组
  各建一个游戏世界 → HUD 上叠 9 套孤儿控件，布局在「没算完」和「又重建」之间横跳。
- **为什么这次才炸**：删掉标题行改变了尺寸通知的时序，把它从「偶尔自己恢复」
  变成「必然无限续期」。
- **修法两处**：① `_layout_orders_below_stats()` 加重试上限（10 次）+ 存活判断
  （离开场景树就放弃）；② `build()` 开头先把上一次的控件清掉，HUD 永远只有一套。
- **状态**：正常启动路径玩家已实测无问题；`check_start` 31/31 仍全绿，
  说明两处守卫没有影响开始界面。`run_tests.tscn` 本身仍是 §4.1 记的过时套件，
  **没有**继续追它的崩溃。

### 上一轮

- **修：站在椅子旁就能清洁**（玩家反馈）
  - 抓到现场：判定区其实是对的（离桌面右沿 17px 才算碰到），
    真正的原因是 **「走到落脚点也算到达」这条兜底对需要触碰的指令也生效**，
    而右侧落脚点被椅子占住（离桌面 68px）。
  - 修法三件套：① 带触碰目标的指令**必须真碰到**才算到达；
    ② 落脚点够不到目标时**换到够得着的一侧**（`_retarget_for_touch`）；
    ③ 可碰判定区从碰撞盒（80×45）改成**整个桌面**（80×60），寻路仍用碰撞盒。
  - 回归：9 个脚本全绿（`check_touch_zone` 9/9、`check_routes` 7/7、
    `check_walk_around` 10/10、`check_ortho` 17/17、`check_angry` 17/17、
    `check_hand2` 23/23、`check_money` 8/8、`check_ticket` 15/15、`diag_clean` 4/4）。

- **加：手上 2 份 + 橙汁**（扩展内容）
  - `Game.hand` 从 `String` 改成 `Array[String]`，容量读 `config.hand_capacity`（=2）；
    容量判断收敛到 `hand_has_room()` / `hand_is_full()` 单一入口。
  - 上菜改为**自动匹配 + 一次送完该桌缺的**，手上不匹配的继续拿着。
  - 饮料机满时**不弹窗**，飘字「你拿不下其他东西了」。
  - HUD 一行文字列出两份；服务员手上**左右肩对称**两个方块。
  - `menu.json` 加橙汁（5 元）。
  - 新增 `tools/check_hand2.tscn`（23 项）与 `tools/check_money.tscn`（8 项）。

- **加：`ItemArt` 贴图接口**
  - 把图片按 id 放进 `assets/items/`（`.png` / `.webp` / `.jpg`）即自动替换占位色块；
    四处（订单票 / 后厨按钮 / 出餐口 / 服务员手上）统一走 `ItemArt`。

### 上一轮

- **修：钱一直是 0**
  - 根因：票送齐时 `order` 被置空（为了票消失），而结账在更晚的「走到门口」时刻读 `order`。
  - 修法：金额独立存 `Table.bill_amount`（`open_order()` 时算好）；
    并**改为「客人一起身就结账」**（已与玩家确认）。

- **修：只上 1 杯可乐就划红线**
  - 根因：`done or (total > 1 and left < total)` 把「送了一部分」和「送完了」混为一谈。
  - 修法：只有全部送完才划线；部分送达改为减数量 + 右侧「还差N」。

- **修：气走后桌子变脏**
  - 根因：`leave_all(true)` 已把桌子设成干净，客人走到门口时 `member_vacated()`
    又执行「全空就变脏」。
  - 修法：`member_vacated()` 先判断桌子是否仍处于 `OCCUPIED`。

### 更早

- **修：客人从桌子上走过去**
  - 三层原因：① 客人需要寻路（左侧座位在桌背）；② A* 自己会返回斜线段；
    ③ 直角化必须在**进树之后**算（否则 `get_world_2d()` 拿不到物理世界，射线全失败、静默失效）。
  - 中途走过弯路：曾给客人另写一套「客用网格 + 直角化 + 安全拐角」，引入多个新 bug
    （路径退化成直线、拐角贴桌沿、客人从原点出发）。**最终改为直接复用服务员那套寻路**。

- **修：订单栏挡住状态栏**；**修：票挡住最左边的桌子**（桌子改为等间距居中）。

- **修：靠近桌椅出现斜线**（格子中心拐点 + 共线合并容差 → 加 `_orthogonalize`）。

- **修：服务员擦桌体积重叠**（网格外扩改成「身体半径 + 2」）。

- **修：点空地撞桌子不绕开**（落脚点一律算出，寻路对所有指令生效）。

---

## 7. 我怎么验证（给自己的规矩）

1. **改完代码先跑 `check_scripts.gd`** —— 只能证明「能编译」，证明不了「方法存在」。
2. **改坐标后必跑 `check_routes`** —— 把每件家具的站位真走一遍，
   最容易的失误是把站位推进碰撞盒（表现就是「点了没反应」）。
3. **改移动 / 寻路后必跑 `check_ortho` + `check_walk_around`。**
4. **测量与现象冲突时，先加日志抓现场，别改代码。**
   这一条是被反复教训出来的：本轮和上一轮各有一次「按推断改了两轮都没中，
   加日志一次定位」。
5. **改完必须重启游戏再让人测** —— 曾出现过「改完没重启就交付」，
   玩家测的是旧进程，白费一次验证。
6. **别写会串状态的复现脚本** —— 先等空闲、再换没被碰过的对象，否则结论完全错。
