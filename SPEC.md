# 餐厅物语 · 功能规格（SPEC）

> 本文件描述**当前代码实际实现的行为**，不是设计意图或待办。
> 凡是与原始需求文档《餐厅玩法详细.txt》不一致的地方，都在文中用 **【偏离】** 标出。
>
> 工程：Godot **4.7.2**，2D，GL Compatibility，1280×720，viewport 拉伸 `canvas_items`。
> 场景树由脚本搭建（`scenes/main.tscn` 只有一个挂 `level.gd` 的根节点）。
> 操作：**仅鼠标左键**。

---

## 1. 场景布局

数据来源：`data/layout.json`（坐标 = 左上角，尺寸 = 宽×高）。视口 1280×720，墙高 **96px**。

### 1.1 家具

| 物件 | rect（x, y, w, h） | 说明 |
|---|---|---|
| 桌1 | `270, 400, 80, 60` | 自带 2 把椅子 |
| 桌2 | `640, 400, 80, 60` | 同上 |
| 桌3 | `1010, 400, 80, 60` | 同上 |
| 后厨 / 出餐口 | `420, 96, 240, 100` | 贴墙（y=96 即墙底边） |
| ├ 出餐口小矩形 | `498, 151, 84, 40` | 嵌在大矩形**内部**，是独立热区 |
| 饮料机 | `845, 96, 110, 100` | 贴墙 |
| 垃圾桶 | `180, 640, 60, 60` | |
| 门口 | `560, 640, 120, 40` | 客人出生点 / 回家点都在 `620, 600` |
| 服务员出生点 | `560, 500` | |

**布局约束（改坐标时必须同时满足，已写进 layout.json 注释）**：
1. 避开**左侧订单栏**（最多 3 张票，约占 `x < 190`、`y 230..470`）；
2. 避开**顶部后厨 / 饮料机**（`x 420..940, y 96..196`）；
3. 避开**底部垃圾桶 / 门口**（`y ≥ 640`）。

### 1.2 座位（椅子）

每张桌自带 2 个座位，`seats[].pos` 是**相对桌子左上角**的偏移：

| 座位 | pos | size | facing | 含义 |
|---|---|---|---|---|
| 左 | `-28, 30` | `40, 30` | `-1` | 客人在桌左、面朝右 |
| 右 | `108, 30` | `40, 30` | `+1` | 客人在桌右、面朝左 |

几何关系：桌子 80 宽，椅子半宽 20 → 椅子与桌沿各留 **8px 缝**（该缝**不可通行**，见 §6）。
客人坐在**靠桌那一侧**（`sit_offset` 再往桌内偏 4px）。

### 1.3 每张桌的碰撞形状

`Table.setup()` 建立 **3 个**碰撞形状，顺序固定：

| 序号 | 形状 | 局部矩形 | 用途 |
|---|---|---|---|
| 0 | **桌面** | `(0, h*0.25, w, h*0.75)` = 80×45 | 寻路障碍（唯一被喂进网格的） |
| 1 | 左椅 | `pos - size/2`，40×30 | 寻路障碍 |
| 2 | 右椅 | 同上 | 寻路障碍 |

**桌面碰撞盒与「可碰判定区」故意不同**：
- **碰撞盒**（序号 0，80×45）→ 喂给寻路网格，动它会牵动座位可达性等一系列已验证行为；
- **可碰判定区**（`Table.touch_box()` = 整个桌面 80×60）→ 上菜 / 收拾的到达判定。

这么分是因为：判定若用碰撞盒（少了最上面 15px），站在椅子旁边会被误判成碰到桌子，
而站在桌子正上方反而判定不到。

---

## 2. 数据结构

### 2.1 三个 JSON（`data/`）

| 文件 | 内容 | 改它能做什么 |
|---|---|---|
| `config.json` | 关键数值（键名与文档第十九节一致，`_` 前缀是注释） | 调耐心、速度、时长、容量、**每桌主食份数** |
| `menu.json` | `kitchen_items` / `drink_items`，每条含 `id / name / price / color`（后厨菜还有 `main`） | 加菜、加饮品、改价、改占位色、**指定哪些是主食** |
| `layout.json` | 全部家具坐标、座位、站位点 | 挪家具、加桌子 |

### 2.2 核心运行时对象

| 对象 | 类型 | 职责 |
|---|---|---|
| `Config` | autoload | 读三个 JSON，提供 `num()` / `item_price()` / `item_name()` / `item_color()` / `roll_group_size()` |
| `Game` | autoload | **唯一持有进度的地方**：钱、已接待数、气走数、玩家手上、`Kitchen`、桌子登记处 |
| `UI` | autoload | HUD / 弹窗，拥有「暂停」 |
| `Stickers` | autoload | 世界飘字（最多 5 条） |
| `DebugTrace` | autoload | 诊断日志（仅当工程根目录存在 `DEBUG_WALK` 文件时启用） |
| `Order` | RefCounted | 一张订单：`items` + `delivered` |
| `OrderLine` | 纯函数 | 票怎么折行 / 划线 / 减数量 |
| `Kitchen` | RefCounted | 后厨队列、计时、出餐口阻塞（纯逻辑） |
| `TableRules` | 纯函数 | 桌子三态规则 + **「这张桌现在能不能坐人」**（`is_seatable`，收口「正在收拾」） |
| `Seat` | RefCounted | 一个座位：`pending` / `got` / `occupant` |
| `Pathfinder` | Node（`pathfinder` 组） | 网格 A*，**两份网格**（服务员 / 客人） |
| `ClickRouter` | Node | 点击 → 走过去 → 到达时再检查 |

### 2.3 玩家手上（`Game.hand`）

```gdscript
var hand: Array[String] = []        # 最多 hand_capacity() 份
```

| 方法 | 语义 |
|---|---|
| `hand_capacity()` | 容量，读 `config.hand_capacity`（默认 2） |
| `hand_has_room()` / `hand_is_full()` | **容量判断的唯二入口** |
| `hand_take(id)` | 加一份；满了返回 false |
| `hand_remove(id)` | 拿走一份（送达） |
| `clear_hand()` | 清空（垃圾桶） |
| `hand_name()` | 显示名，如「汉堡、可乐」；空手时「空手」 |

> **设计要点**：容量判断只留一个入口。原先「手上只有一份」的假设散落在取餐 / 饮料机 /
> 上菜 / 垃圾桶多处，改成 2 份后若各写各的，漏改一处就会出现
> 「拿了 1 份以后再也拿不了第二份」。

### 2.4 桌子状态（`TableRules.State`）

| 状态 | 值 | 可坐人 | 可点击的理由 |
|---|---|---|---|
| `CLEAN_EMPTY` | 干净空桌 | ✅ | 否（没客人、不脏） |
| `OCCUPIED` | 有人 | ❌ | 有客人在等点单时：接单 |
| `DIRTY` | 待收拾 | ❌ | 收拾 |

`Table` 另有：`order`（共享订单，送齐后置空）、`bill_amount`（该收多少）、
`meal_completed`（是否吃完，气走为 false）、`_bill_settled`（只结一次）。

---

## 3. 客人状态机

`Customer.state` 与文档第四节一一对应。耐心环从 12 点钟方向顺时针缩短。

| 状态 | 进入条件 | 耐心 | 头顶显示 | 可点击 | 超时行为 |
|---|---|---|---|---|---|
| `WALKING_IN` | 生成于门口 | — | 无 | ❌ | — |
| `WAITING_TO_ORDER` | 走到座位坐下 | **28s** | 耐心环 + `!` | ✅ | 气走 |
| `ORDER_TAKEN` | 玩家点客人 / 点桌子接单 | **45s** | 耐心环 | ✅ | 气走 |
| `ORDERED` | 后厨 UI 点「完成」 | 45s（**不重置**） | 耐心环 | ✅ | 气走 |
| `EATING` | 自己那几样全部上齐 | **12s** | 「用餐中」 | ❌ | 吃完 |
| `LEAVING` | 整桌都吃完 → 桌子统一叫走 | — | 无 | ❌ | 走到门口后消失 |
| `ANGRY_LEAVING` | 耐心归零 | — | 无 | ❌ | 走到门口后消失，**不留脏桌** |

**关键规则**：

1. **订单在入座时就已存在**（客人随机点好，1~2 样、允许重复），玩家接单只是「揭晓」。
   所以接单不需要任何选择 UI。
   但**点单规则受「每桌至少 1 份主食」约束**，见 §4.1 —— 整组订单是
   在**客人生成那一刻**一起摇好的（不是各自摇、也不是入座时才摇）。
2. **同桌共享订单**：订单挂**桌子**上，内容 = 同桌所有座位 `pending` 的拼接。
3. **整组一起来、一起走**（已与用户确认）：
   - 各人 12s 用餐计时**独立**，先吃完的**留在座位上等**；
   - 整桌都吃完 → `Table.leave_all(false)` 统一叫走；
   - 一人气走 → **整桌一起气走**（`on_customer_angry` → `leave_all(true)`）。
4. **座位到门口才归还**：起身时不还座位（否则桌子会立刻判「没人」而变脏，
   而客人还在店里走）。`member_vacated()` 在走到门口时释放。
5. 客人**不是**物理物体（否则会挡住服务员），而是用「一个点 + `TOUCH_RADIUS`(50)」判定接触。

**`TOUCH_RADIUS = 50` 的由来**：客人坐实体椅子上，服务员最多走到椅子外沿
（椅子半宽 20 + 服务员半径 16 = 36），格子寻路再偏出约 10px（半格）→ 取 50 留余量。
**它必须 ≥ 椅子半宽 + 服务员半径 + 半格**，否则会出现「贴着椅子却接不了单」。

---

## 4.1 客人怎么点单（「不能只点饮料」）

| 项 | 规则 |
|---|---|
| 每位客人点几样 | `item_count_weights`：1 样 50% / 2 样 50%（上限 `max_items_per_customer`） |
| 从哪抽 | **全菜单**（后厨 + 饮品），允许重复 —— 所以「两份汉堡」「汉堡+可乐」都可能 |
| **主食下限** | 每**桌**至少 `min_main_per_group` 份主食（默认 1；设 0 = 关闭） |
| 哪些算主食 | `menu.json` 里带 `"main": true` 的后厨菜。当前：汉堡 15 / 薯条 6 / 炸鸡块 12 |
| 饮品 | **永远不是主食**（`is_drink` 的一律 `is_main = false`） |
| 不满足怎么办 | **整组重摇**（不是只补摇某一位），上限 `group_order_reroll_max`（默认 20） |

**按「整桌合计」算，不按人算**（已与用户确认）：
一组两人允许「一人汉堡 + 可乐、另一人只喝可乐」；
但**整桌全是饮料是不允许的**，两人都只点饮料会被重摇掉。

> **【为什么必须整组一起摇】**
> 成员若各自摇、事后补摇某一位，「谁被迫点主食」就永远落在同一个人身上
> （比如总是后入座的那位），客人之间机会不平等。整组一起摇 + 整组重来，
> 每个人被抽中的概率一致。
>
> **【为什么不把饮料改成主食】**
> 那样会把「可乐+汉堡」变成「汉堡+汉堡」，既丢了原本点的东西，
> 又把主食占比抬得比设计值高。重摇能保留原来的 1~2 样分布。
>
> **【无解的兜底】** 菜单里一道主食都没有 → 不重摇、直接按普通规则摇
> （否则就是死循环）。摇满上限仍未满足（概率极低）→ 给前几位客人
> 各**换掉单里最后一样**为主食，保证规则永远成立。两条都有测试覆盖。

**重摇的代价有多大**：全菜单 5 道里 3 道是主食 → 单人摇出「只有饮料」的概率约
(2/5) + (2/5)² ≈ 56%… 实测（4000 次×2 组）重摇几乎总是一次就过，
单次 `roll_group_orders` 的开销可以忽略。

---

## 4.2 一组客人怎么进场

| 项 | 规则 |
|---|---|
| 生成时机 | **同一帧把整组全部创建**（已与用户确认，原先是「前一位落座后下一位才生成」） |
| 各自出发点 | 沿门口 x 轴错开 `group_entry_stagger`（默认 36px）：第一位站门口中心，第二位偏开 |
| 走路 | 各走各的寻路路径到自己的座位（门口没有碰撞体，错开纯属视觉） |
| 订单 | 在生成那一刻就整组定好，入座 / 接单都只是揭晓 |
| **下一组什么时候能进** | 只在「场上没有客人处于 `WALKING_IN`」时才开新组 |

> **【判断「上一组走完没有」必须查状态，不能用计数器】**
> 这里曾经有一个 `_group_size` 计数器，只在 `spawn_group()` 末尾赋值、
> **没有任何地方清回 0**，于是 `if _group_size > 0: return` 永远成立 ——
> **整局只来第一组客人**（玩家实测报的 bug）。不报错、不警告。
> 现在一律查场上客人的 `state`：它由客人自己维护、随落座自动消失，
> 不存在「谁忘了复位」。
>
> **【`Door.begin_group()` / `end_group()` 必须是一组一次的配对】**
> `end_group()` 若落进生成循环里，等于每生成一位就把序号复位 →
> 两位客人门口起点重叠（错开量实测 0px）。本轮踩过。

> **【为什么从「依次进场」改成「同时生成」】**
> ① 用户明确要求；② 整桌订单要一起校验、一起重摇 ——
> 成员先后生成的话，「整桌缺主食」只能事后补摇某人，就回到上面那个不公平问题。
>
> **【门口的错开是必须的】** 客人不是物理物体，从同一个点出发会让两个人的圆
> 完全重叠着走进店里，看起来像一个人。错开由 `Door.begin_group()` /
> `next_group_spawn()` / `end_group()` 三件套管理（一组一复位）。
>
> **【改起点为什么不复用 `setup()`】** `setup()` 还负责
> `reached_seat` / `gone` 两个信号连接；已经连过再走一遍会让两个回调**各触发两次**
> （座位被记两次、订单栏刷两次），而且这种重复连接不报任何错。
> 所以另开了 `Customer.place_at()`：只挪位置 + 重算进场路径。

---

## 4. 点单流程

```
客人从门口走进来（走寻路，不从桌上穿过）
  ↓ 到达座位 → take_order()：把自己的菜记在 Seat.pending，进入 WAITING_TO_ORDER
  ↓ 头顶出现 ! 和耐心环（此时看不到内容）
玩家点客人（或点这张桌子）
  ↓ 点击瞬间检查：客人还在等点单？
  ↓ 走过去（到达判据：身体碰到客人）
  ↓ 到达时再检查：还在等点单？桌子有效？
  → Table.open_order()：把同桌所有 pending 拼成一张票，挂到桌上
  → 同桌每位客人 accept_order()：进入 ORDER_TAKEN，耐心重置为 45s
  → 订单栏出现「桌N」
```

> **票不是「有人来了」的通知，而是「你答应做这件事了」的记录** —— 开局左侧是空的。
> 座位一旦空出，桌子不再挂订单。

**点桌子 = 点客人**：正在等点单的桌子被点击时，`Table.interact()` 转交给那位客人执行。

---

## 5. 后厨逻辑

`Kitchen` 是纯逻辑对象（可单测），规则：

| 规则 | 说明 |
|---|---|
| **一道一道做** | 按 `queue` 顺序（玩家在后厨 UI 的点击顺序），**每道都要完整的 3 秒** |
| 每道 **3s** | `kitchen_cook_time` |
| 出餐口容量 **2** | `counter_capacity`（**已从 1 改成 2**，见下） |
| **不阻塞** | 出餐口满了**不会**让后厨停工，做好的菜先"预存"，出餐口一空出来**当帧**补上 |
| 预存区上限 | `_cooked` 最多堆 `counter_capacity` 份；堆满才真的暂停做菜 |
| 气走不清理队列 | 客人气走后后厨继续做 |

> **【「一道一道做」是硬要求：别写成「做完立刻接下一道」】**
> 这里连续错过两版，都是"改测试而不是改逻辑"的后果：
> ① `timer = _cook_time if not queue.is_empty() else 0.0` —— 摆出第一份时队列非空，
> 于是白白多等一个完整周期（3 份要 12 秒）；
> ② 改成 `timer = 0.0` 想修 ① —— 但 `update()` 第一件事就是判断 `timer <= 0`，
> **下一帧又出一份**，玩家看到的正是「一起下单的菜同时做好、同时出现」。
> 正确写法：**做完一份、若还有下一道，计时重置为完整的 `_cook_time`**。
> 实测时间线：`t=3.0s` 完成第 1 道、`t=6.0s` 完成第 2 道（间隔 3.0 秒）。

### 5.2 后厨点单弹窗（`scripts/ui/kitchen_popup.gd`）

**布局**（2026 改版，玩家指定；目的是**便于日后放美术资源**）：

```text
             后厨 · 点餐台
  左键点击餐品加入制作队列；右键点击队列中的菜品可将其删除

   ┌────┐   ┌────┐   ┌────┐
   │ 🍔 │   │ 🍟 │   │ 🍗 │     ← 96×96 方块槽（菜品图标 + 右上角「已点 N 份」角标）
   └────┘   └────┘   └────┘
   汉堡 15元  薯条 6元  炸鸡块 12元   ← 菜名 + 价格，合并成一行

   队列： ■ ■ ■ ■ ■ …            ← 本次已点、待提交，按点击顺序；不限个数自动换行

        [ 完成 ]   [ 取消 ]
```

| 交互 | 行为 |
|---|---|
| **左键**点菜品方块 | 该菜 **+1**（没有 −/+ 按钮；方块右上角角标显示已点份数） |
| **右键**点队列里的小方块 | 删掉**被点的那一份**（左键点它什么也不做，避免误触） |
| 悬停队列小方块 | tooltip「右键可删除该餐品」（只是文字提示，不拦截点击） |
| 完成 | 队列为空 → 只提示「请先点菜」、**不关 UI**；否则按点击顺序入队后关闭 |
| 取消 | 清空队列并关闭 |

**实现要点**（换美术时照这几条走）：

* 菜品图标走 `ItemArt.make_node(id, ...)` —— 往 `assets/items/<id>.png` 放图即自动生效，
  没图就退回 `menu.json` 的 `color` 占位色块；**面板与坐标都不用改**。
* 方块槽是「固定 96×96 容器 + `clip_contents`」+ 自绘 `Panel` 底 + 图标 + 角标。
  **不要用 `Button.icon + expand_icon`**：它把图标塞进"文字之外剩下的高度"，
  96px 的按钮里图标只剩几像素，方块根本看不见（截图确认过）。
* 队列的逐份顺序存在 `_queue: Array[String]`（如 `[burger, burger, fries]`）。
  **不是按种类记数量** —— 按种类记就没法表达"删掉第 1 个汉堡还是第 2 个"。
* 队列小方块是独立控件 `scripts/ui/queue_tile.gd`，自己用 `_gui_input` 处理右键，
  删完发 `remove_requested` 信号给弹窗（弹窗才知道队列的数据结构）。
  **它的子节点必须 `mouse_filter = IGNORE`**，否则右键落在贴图上时方块收不到事件。

> **【坑：悬停浮现不要切 `visible` —— 已因此放弃"悬停删"方案】**
> 交互一开始做的是「鼠标指到小方块上，右上角浮现红 ✕，点它删」。两版都翻车：
> ① ✕ 放在小方块外侧一半 → 越出父容器的部分**不参与命中测试**、还被右边的邻居盖住；
> ② 改成「✕ 常驻 + 悬停只切 `shown`」后仍有问题：✕ 恰在鼠标下方，
> `mouse_entered/mouse_exited` 在「出现 → 鼠标离开 → 隐藏 → 鼠标回来」之间每帧循环
> （**红 ✕ 闪烁 + 输入延迟，最后一版甚至点不动**）。
> 结论：**控件自己就在鼠标下方时，不要用显示/隐藏来做悬停反馈**。
> 现在改成**右键删除**（玩家指定）：不需要任何额外控件、不改变命中结果，最稳。

回归：`tools/check_kitchen_ui.tscn`（22 项：方块尺寸、点方块累加、右键逐份删除、
左键不误删、子节点不拦鼠标、tooltip 提示、空队列提示、取消清零）。

**后厨 UI 的按钮给谁做菜**：玩家**自由预做**，与客人订单无关。做多了没人要就扔垃圾桶。

**出餐口视觉刷新必须由数据驱动**：`Kitchen` 的出餐口内容私有 + 唯一写入口
`_set_counter()`，变化必定 `counter_changed.emit()`，`KitchenWindow` 订阅后重绘。
（曾经靠鼠标悬停事件刷画面，导致「餐被拿走后方块不消失，要动一下鼠标才更新」。）

> **【订阅必须跟着 Kitchen 对象走】**
> `Kitchen` 是**每开一局重建**的对象（`Game.reset_progress()`），
> 而出餐口节点一直活着。若只在 `_ready` 连一次，新一局之后订阅就挂在旧对象上 ——
> 症状与上面那条一模一样：**取走餐后图标不消失，动一下鼠标才更新**（玩家实测报过）。
> 所以：`Game` 在换 Kitchen 时广播 `kitchen_changed`（**唯一入口**），
> `KitchenWindow._rebind_counter()` 收到就重绑。任何新的「订阅 Kitchen」的节点
> 都必须照这个模式做。

### 5.1 为什么改成「不阻塞 + 出餐口 2 份」

**玩家要求**：「只要玩家点单，后厨就开始制作，而不是等取走一份再做下一份」。

原设计是**阻塞式**：出餐口有餐 → `update()` 直接 return（连计时都停），
于是玩家不回来取餐，第 2 道菜连开始做都不会 —— 后厨被玩家的取餐动作牵着走。

**现在的分工（两个住处，但总是一个真相）**：

| 集合 | 含义 |
|---|---|
| `queue` | 还没开始做的（玩家下的单） |
| `_cooked` | **已经做好、等出餐口位置**的 |
| 出餐口 | 玩家能立刻拿走的（最多 `counter_capacity` 份） |

**视觉上自洽**：出餐口永远优先摆满；玩家取走一份 → 空位当帧被填满 →
看起来就是「它一直在那儿排队」。玩家不会困惑，因为**看不到隐藏状态**
（`_cooked` 只在出餐口真的满时才存在，而那一刻看得见的 2 份就是能拿的）。

> **【HUD 的「后厨队列」用的是 `pending_count()`】** = `queue + _cooked`。
> 只数 `queue` 会出现「后厨队列：0」却还在不断冒出新菜。
>
> **【踩过的坑：摆出第一份后白白多等 3 秒】**
> 原来写成 `timer = _cook_time if not queue.is_empty() else 0.0` ——
> 摆出第一份时队列非空，计时被重置成**满 3 秒**，于是 3 份菜要 12 秒而不是 9 秒。
> 正确语义是「一道做完、位置腾出来，下一道**立刻**开始」，所以 `timer = 0.0`。

**取餐规则**（已与玩家确认）：**一次把出餐口能拿的都拿走**（受手上容量限制）。
所以手上为空时点一下能拿 2 份；手上已有 1 份时只能再拿 1 份。

---

## 6. 寻路与移动

### 6.1 网格

`Pathfinder` 建立**两份**网格（`CELL = 20.0`，视口 64×36 格），4 方向（**不走斜线**），
启发函数用曼哈顿距离。

| 网格 | 外扩量 | 给谁用 | 为什么 |
|---|---|---|---|
| 服务员 | `WAITER_RADIUS + 2` = **18px** | `find_path()` | A* 给的路径本身保证身体不碰家具，不需要物理去推 |
| 客人 | **8px** | `find_path_for_customer()` | 18px 会把**椅子那格也封死**，客人永远走不到自己座位 |

**障碍来源**：每件家具的**每一个**碰撞形状**分别**作为障碍（桌子、左椅、右椅各一个）。
**不要用「桌子+椅子合并成一个矩形」** —— 合并会把椅子那块也封死，座位在网格里不可达，
目标点落在障碍内部，A* 只能给斜线（玩家反馈的「靠近桌椅就出斜线」）。
桌子与椅子之间那条 8px 缝靠「外扩 ≥ 身体半径」自动被吃掉，它本来也过不去。

网格是**静态的**（没有任何家具会移动），只在摆完家具后建一次。

### 6.2 到达判定（`Waiter`）

| 判据 | 适用 | 说明 |
|---|---|---|
| **碰到目标物件** | 带 `touch_target` 的指令 | 目标的 `touches_from(pos, RADIUS)` |
| 走到落脚点 | **仅不带触碰目标的指令** | `dist(goal) ≤ ARRIVE_EPS(6.0)` |

> **重要**：带触碰目标的指令**不能**用「走到落脚点」兜底。曾经无条件启用这条，
> 结果从被椅子挡住的一侧点桌子时，A* 把落脚点吸附到椅子外侧（离桌面 68px），
> 服务员走到那儿就算「到达」→ **没碰到桌子就开始清洁**。

**落脚点换点（`_retarget_for_touch`）**：若落脚点够不到目标
（`dist(goal, 最近可碰点) > RADIUS + 3`），就在目标周围 16 个方向 × 2 个半径
找「站上去身体能碰到目标、且 A* 走得到」的格子，找到就换过去 —— 即
**绕到够得着的那一侧**。

**接触几何接口**（`WorldObject` / `EntityBase` / `Customer` 各实现一遍）：

```gdscript
touch_point(from) -> Vector2         # 离 from 最近的可碰位置
touches_from(from, radius) -> bool   # 你碰到我了吗
touch_box() -> Rect2                 # 可碰矩形（家具用；桌子覆盖成整个桌面）
standoff() -> float                  # 站定后再退开多少（当前客人=0、家具=0）
```

### 6.3 拐点整理

`_prune_waypoints()`：丢 <1px 的点 → 合并共线 → **`_orthogonalize()` 强制每段只能是横或竖**
（斜段插一个直角拐角，两种拆法用真实碰撞盒验证，哪个不碰用哪个）。

> **为什么必须正交**：A* 的拐点是**格子中心**，终点为贴住家具会偏移几像素，
> 那一段是「几乎竖直、斜了 4°」。共线合并按方向相近判断（4° 也算共线），
> 会把它和真正的竖直段合并成一条**斜线** —— 玩家看到的就是「靠近桌椅就出斜线」。

### 6.4 客人进场寻路

客人同样走寻路，但**没有碰撞体**（否则挡路），所以斜线就是**硬穿**。
因此客人路径要做三步：

1. 用 `find_path_for_customer()`（8px 外扩网格）；
2. **对整条路径正交化**（A* 自己就可能返回斜线段 —— 终点被吸附到「最近可走格」，
   从那儿连到真实座位往往是斜线）；
3. 末尾接上真实座位点，必要时插安全拐角。

**只查桌子、不查椅子**：客人本来就坐在椅子上，最后一段必然和椅子重叠，那是正常的。

### 6.5 卡住看门狗

| 参数 | 值 | 行为 |
|---|---|---|
| 判定阈值 | 位移 < **1.5px** | 视为没动（正常一帧约 4.3px；阈值太小会把「被轻轻挤一下」误判成卡住，导致频繁重算、路径变锯齿） |
| 重算路径 | `stuck_timeout * 0.6` | 换一条路再试 |
| 放弃指令 | `stuck_timeout` = **2.0s** | 发 `command_cancelled`，由 `ClickRouter` 飘字说明原因 |

### 6.6 撞住自动改道 + 沿面滑行

**为什么需要它（玩家反馈）**：擦到椅子时 `move_and_slide` 会**沿椅面滑**，
一帧还能滑 3~4px —— 6.5 那条「位移 < 1.5px」的判据**看不见**这种状态，
于是路径不重算；等真滑不动了，人已经贴死在椅角上，
玩家必须**再点一次**才会改道（新指令用当前位置重算，路线就不擦椅子了）。

| 参数 | 值 | 行为 |
|---|---|---|
| 判据 | 本帧真实位移 < `speed × delta` × **0.6** | `_blocked_no_progress()`：想走却没走出去 → 立刻重算路径 |
| 重算冷却 | **0.3s** | 防抖：每帧重算会让路线变锯齿 |
| 重算预算 | **6 次**（独立于 `MAX_REPATH`） | 撞椅子是常态操作，不该几次就用完 |
| 预算回收 | 连续走顺 **1.0s** 回收 1 次 | 否则一局里撞多了就退回旧行为 |
| 滑行偏转 | 最大 **18°** × \|sin(入射角)\| | `_slide_steer()`：按真实碰撞法线把速度偏到切向，正撞偏满、擦角不偏 |
| 法线筛选 | 前进方向与法线 `dot < -0.2` 才算「压着这个面」 | 法线是**上一帧**的，侧擦时已过期；不筛会把人往回头方向推 |
| 正撞退化 | 偏转 **0**（切向取成与行进方向同向） | 完全正撞时切向垂直于行进方向，按它偏只会凭空造出横向分量 → 贴着家具 60Hz 左右摆（实测就是玩家报的抖动） |

> **【别用 `slerp`/`lerp` 混合来做这个偏转】** 实测 `Vector2.slerp(a,b,t)` 的 `t`
> 是「离 a 多远」（`slerp(0,-1 → -1,0, 0.75)` 只得 22.5°）；而 lerp 混合再归一化
> 会悄悄改变夹角（叉积反算 23°，真实只有 7°）—— 调参看到的是假数据。
> 所以这里**显式 `rotated()` 按角度转**，量多少就是多少。

回归体检：`tools/check_smooth_walk.tscn`（量「最长连续停滞帧数」，上限 30 帧）。

### 6.7 落脚点净空（防「碰到桌子抖动」）

**为什么必须有它**：落脚点不只是「走哪儿」，它同时决定「到了没有」。
如果它落在**碰撞盒内部**，人就会一直想压进去、每帧被 `move_and_slide` 挤出来 ——
叠加 6.6 的滑行横向分量，就成了玩家看到的「碰到桌子在抖」（60Hz、1.8px）。

`_compute_goal()` 原来按「整团家具的**外接矩形**」退 `RADIUS + 4`，
但外接矩形比真实碰撞盒大（桌1 左椅把左边界拉到 x≈190，桌体碰撞盒左边在 x≈253）：
从下方点桌子时算出的落点离碰撞盒只有 12px，**身体（半径 16）压进桌子 4.4px**。

| 参数 | 值 | 说明 |
|---|---|---|
| `GOAL_CLEARANCE` | **RADIUS + 1 = 17px** | 在**真实碰撞盒**上迭代推出去；17px 正好在可碰判定容差（也是 `RADIUS + 1`）之内，所以「站上去就够得着」，指令能正常结束 |
| 适用范围 | 带触碰目标 **和点空地** | 点空地原来完全没做净空（玩家点在桌沿上时最严重） |
| 可碰判定容差 | `RADIUS + extra + 2` | 贴上去被挤在 1~2px 之外时也算到达，避免「差 1px 判没碰到 → 再走一步 → 被挡住」的循环 |

回归体检：`tools/check_no_jitter.tscn`（模拟连点，量「方向来回帧数」，上限 6；
把 `GOAL_CLEARANCE` 改回 0 会立刻变红 156~222 次，证明它抓得住这个 bug）。

---

## 7. 取餐与上菜

### 7.1 取餐（出餐口）

```
点出餐口小矩形
  ↓ 点击时检查：① 手上还有空位 ② 出餐口有餐
  ↓ 走到 pickup_walk
  ↓ 到达时再检查同样两条
  → take_from_counter() → router.take_into_hand(id)
```

**前置条件是「手上还有空位」而不是「手上为空」** —— 写错会变成「拿了 1 份后再也拿不了第二份」。

**一次点拿几份**：**把出餐口能拿的都拿走**，受手上剩余空位限制
（手上空 → 一次 2 份；手上已有 1 份 → 只能再拿 1 份）。
取走的同时出餐口会**当帧**被预存的菜补满（见 §5.1）。

### 7.2 饮品（饮料机）

```
点饮料机
  ↓ 手上满了 → **不弹窗**，飘字「你拿不下其他东西了」
  ↓ 走过去 → 到达时再检查 → 弹饮料机 UI（此时游戏暂停）
  ↓ 选一杯 → 进手上 → 走到客人身边上菜（与汉堡完全同一条路径）
```

### 7.3 上菜

```
点客人 / 点桌子
  ↓ 点击时：手上非空？手上有**至少一样**是这桌缺的？
  ↓ 走到桌边（到达判据：身体碰到**桌子**，不要求走到客人身边）
  ↓ 到达时**再检查**：手上现在还有哪些是这桌缺的
  → 一次把该桌缺的、手上有的**都送完**
  → 每份交给 Table.assign_delivery()：给「还没拿到这道菜」的第一位客人
  → 该客人齐了 → start_eating()
  → 整桌票送齐 → table.order = null（票消失）
```

**已确认的规则**：
- **自动匹配**：那桌缺哪样就送哪样，玩家不用选；
- 手上**不匹配的那份继续拿着**，等下次点击送别桌；
- **一次点击把该桌缺的都送完**（两人桌点汉堡+可乐，手上两样都有 → 一次送齐）；
- **手上扣几份以「实际送出去几份」为准** —— 见下面的坑；
- 空手 vs 拿错菜的提示要分开（空手说「这桌没点这个菜」会让玩家以为是菜点错了）。

> **【坑：手上两份同款、桌上只要一份时，两份都被扣掉】**
> `_wanted_in_hand()` 是**按手上的份数**列出来的，会带重复：手上两份汉堡、
> 而这一桌只缺一份时，它返回 `["burger", "burger"]`。
> 早先的循环是「`assign_delivery(item)` + 无条件 `remove_from_hand(item)`」——
> 第一遍真的送出了，第二遍 `assign_delivery` 已经找不到想要的客人
> （返回 **null**），但返回值被丢掉、照样扣一份 → **两份汉堡全没了**。
> 现在先数出「实际送成功了几份」（唯一真相是 `assign_delivery` 的返回值），
> 再按这个数扣手上的菜：`Customer._deliver_items()` / `_apply_delivered()`。

### 7.4 点桌子时的提示文案（判断顺序很关键）

`Table.interact()` 的**分支顺序**决定了玩家看到哪句话，顺序本身是规格的一部分：

| 顺序 | 条件 | 提示 |
|---|---|---|
| 1 | 桌子是 `DIRTY` | （去收拾，无提示） |
| 2 | 有客人在等点单 | （等价于点客人，去接单） |
| 3 | **桌上一个客人都没有** | `MSG_TABLE_EMPTY`「这桌是空的」 |
| 4 | 有客人 + 手上有这桌缺的菜 | （去上菜） |
| 5 | 有客人 + 手上有菜但没人要 | `MSG_WRONG_DISH`「这桌没点这个菜」 |
| 6 | 有客人 + 空手 | `MSG_HAND_EMPTY`「手上没有餐品」 |

> **【顺序踩过的坑】「手上有菜」曾经排在「桌上有没有人」前面**，于是
> **手里拿着菜去点一张空桌**会得到「这不是他要的菜」——
> 桌上一个客人都没有，这句话完全不成立（玩家实测报的 bug）。
> 「桌上有没有人」比「手上是什么菜」更基础，**必须先问**。
>
> **【文案改过一次】**「这不是他要的菜」→ **「这桌没点这个菜」**。
> 原句是从客人视角说的，而点击的主体是「这桌」；改后与「这桌是空的」成一套。
> 两句话问的是不同问题，不能混用：前者=桌上没人，后者=有人但不要这个。
> 文案常量：`Constants.MSG_TABLE_EMPTY` / `MSG_WRONG_DISH` / `MSG_HAND_EMPTY`。

---

## 8. 用餐与结账

```
全部上齐 → EATING（12s）
  ↓ 各人计时独立，先吃完的留座等
  ↓ 整桌都吃完 → Table.meal_completed = true → leave_all(false)
  → **在这一刻结账**：Game.add_money(bill_amount)，飘字「+N 元 · 结账」
  → 各自走向门口
  → 走到门口 → member_vacated()：还座位；**最后一位**到门口时桌子变 DIRTY
```

**金额来源**：`Table.bill_amount`，在 `open_order()` 时就算好并存下。

> **【重要修正】原来结账挂在「走到门口」那一刻，导致钱永远是 0。**
> 原因是票送齐时会把 `order` 置空（那样订单栏的票才会消失），
> 而结账比置空晚 —— `if order == null: return` 直接跳过结账。
> 现在金额独立存放，且**改为「一起身就结账」**（已与用户确认），
> 时机更直观，也不依赖 `order` 是否还在。

---

## 9. 收拾

| 项 | 值 |
|---|---|
| 触发 | 点 **DIRTY** 的桌子 |
| 到达判据 | 身体碰到**整个桌面**（`touch_box()`） |
| 耗时 | **2.0s**（`clean_time`）**【偏离：文档写 3.0】** |
| 进度条 | 服务员头顶，绿色条 |
| 可取消 | ❌ **不可取消**（`_locked`，期间点任何东西都无效） |
| **收拾期间该桌不可坐** | ✅ 见下 |
| 完成 | `finish_clean()` → `CLEAN_EMPTY` + `mark_available()`（**有人在座时除外**） |

**「碰到桌子才触发」的实现要点**：
- 判定区 = **整个桌面 80×60**（不是碰撞盒 80×45）；
- 落脚点够不到时**绕到够得着的一侧**（`_retarget_for_touch`）；
- 带触碰目标的指令**必须真碰到**才算到达。

这三点缺一条就会出现「站在椅子旁边就开始清洁」。三条缺一不可，都是实际踩出来的。

### 9.1 「正在被收拾」是一个跨模块状态（`TableRules.is_seatable`）

**收拾进度只存在服务员身上**（`waiter.cleaning_table`），桌子自己一直是 `DIRTY`。
而**选座发生在更早的时刻** —— 玩家点下去那一瞬间桌子还是 `CLEAN_EMPTY`。
两者对「这张桌现在能不能坐人」没有共识，于是出现过一个玩家实测报的 bug：

```
点脏桌 → 服务员走过去（这几帧里桌子已经不是 DIRTY）→ 客人直接落座
→ 收拾完成 finish_clean() 无条件把桌子设回 CLEAN_EMPTY
→ 结果：椅子上坐着人，桌子却对外宣称「干净又没人」
   （玩家点它看到「这桌是空的」；后来者还会和它挤同一个座位）
```

**修法**：

| 关注点 | 唯一入口 |
|---|---|
| 这张桌能不能坐人 | `TableRules.is_seatable(table)` —— 排除「正在收拾」+ 要求 `CLEAN_EMPTY` |
| 「正在收拾」的登记 | `waiter.begin_clean()` 登记 / `waiter._end_clean()` 解除（**成对**，中断路径也会解除） |
| 完成时的兜底 | `Table.finish_clean()`：**有人在座就保留 `OCCUPIED`**，绝不洗成空桌 |

选座（`SeatManager`）、HUD 空桌数（`Game.available_tables()`）、全店空位统计
三处全部改走 `is_seatable`，不再各自判断 `state`。

> **【为什么解除登记放在 `_end_clean()` 而不是 `finish_clean()` 之后】**
> 收拾可能被中断（桌子被释放等）。登记若没被解除，那张桌会**永久不可坐** ——
> 比原 bug 更难查。所以解除与登记严格配对，放在一定会走到的收尾函数里。
>
> **【`finish_clean()` 的守卫为什么必要】** 登记已经让客人坐不进来，
> 但它是最后一道防线：万一状态因为别的原因错乱，宁可桌子保持「有人」，
> 也不能对外宣称「干净又没人」。

---

## 10. 气走

触发：`WAITING_TO_ORDER` / `ORDER_TAKEN` / `ORDERED` 期间耐心归零。

```
go_angry() → table.on_customer_angry() → leave_all(true)
  → Game.angry_count += 1
  → **整桌一起走**（不是只有他没耐心）
  → **不结账**（收入 0）
  → 座位一次性清空、order 置空、桌子立刻回到 CLEAN_EMPTY
  → **不留脏桌**（文档第十节）
```

> **已修过的 bug**：`leave_all(true)` 已经把桌子设成干净，可是客人走到门口时
> `member_vacated()` 又执行「全空就变脏」，把干净的桌子改脏了。
> 现在 `member_vacated()` 先判断「桌子是否仍处于 OCCUPIED」——
> 只有吃完离场才变脏。

**后厨不受影响**：气走不清理队列，已经下的菜继续做。

---

## 11. 垃圾桶

| 项 | 行为 |
|---|---|
| 触发 | 点垃圾桶 |
| 前置 | 手上有餐（`hand_is_empty()` 为空 → 飘字「手上没有餐品」） |
| 行为 | 走到站位 → **一次清空手上所有** |
| 用途 | 玩家自由预做多了、没人要的餐 |

---

## 12. 收入

**钱只有菜钱，没有小费 / 口碑 / 罚款。**

| 菜品 | 价格 |
|---|---|
| 汉堡 burger | **15** |
| 薯条 fries | **6** |
| 炸鸡块 chicken | **12** |
| 可乐 cola | **3** |
| 橙汁 juice | **5** |

- 结账时机：**整桌吃完、一起起身那一刻**，一次结清整桌（不按人分次）。
- 气走：收入 **0**（不需要额外扣钱 —— 少赚一笔本身就是惩罚）。
- 同桌只结一次（`_bill_settled`）。
- **钱在每一关开始时归零**（见 §12.1「每关独立」），所以「本关营收」= 钱。

**餐品占位色**：汉堡 `c0392b`（红）/ 薯条 `f2c230`（黄）/ 炸鸡块 `b5651d`（焦糖褐）/
可乐 `3f6fb5`（蓝）/ 橙汁 `e8811f`（橙）。

---

## 12.1 关卡模式（一局一局）

**一局 = 一关**：在 `time_limit` 秒内赚到 `target_money` 元就算过关。
关卡表在 `data/levels.json`，加关卡只改 JSON。

| 关 | 名称 | 时长 | 营收目标 | 元/秒 |
|---|---|---|---|---|
| 1 | 试营业 | 90s | 60 | 0.67 |
| 2 | 午市 | 120s | 80 | 0.67 |
| 3 | 晚高峰 | 120s | 90 | 0.75 |
| 4 | 周末 | **130s** | 100 | 0.77 |
| 5 | 爆单日 | 150s | 120 | 0.80 |

> **【曲线是平缓递增的】** 0.67 → 0.67 → 0.75 → 0.77 → 0.80 元/秒，
> 而玩家实测上限约 **1.0 元/秒**，所以每关都留有空间。
> **L4 的时长本轮放宽到 130 秒**（120 → 130），好让它的三星强度与 L3 同档。
>
> **【L3 起新增饮品「柠檬水 2 元」】** 它把客人平均单价拉低，
> 所以 L3 除了目标更高，还多了"同样金额要送更多趟"这一层。
> 菜单按 `start_level` 渐进解锁（见 §19.2）。

> **【这些数字怎么定的】** 不是理论推算，是**按玩家实测**定的 ——
> 理论吞吐（人均 7.8 元 × 全部客人结账）会明显偏高，因为
> **客人等不到会气走、玩家来回跑腿也有空档**。
> 调难度只改 `target_money` / `time_limit`，代码不用动。

**已确认的设计决定**：

| 问题 | 决定 |
|---|---|
| 一局怎么结束 | **固定时长**，时间到即结算（不做中途失败条件） |
| 成绩指标 | **只看营业额**（钱只增不减，气走＝少赚） |
| 营收怎么算 | **每关独立**：进关时营收归零，赚到目标就算过 |
| 过关后 | 结算界面「下一关」 |
| 未过关 | 结算界面「重试本关」（不惩罚、不丢进度） |
| 最后一关过了 | 没有第 6 关 → 按钮变「重玩第 1 关」，回第 1 关 |
| 进度存盘 | **不存**：只在本次运行内有效，关游戏从第 1 关开始 |
| **星级** | **每关 3 档门槛**（见 §12.2）；结算界面画星星，HUD 顶部正中用三颗星显示目标与达标情况 |
| 结算界面显示 | 大标题（通关成功/失败）+ **星星** + 营业额（含目标）+ 接待客人总数 + 一个按钮 |

### 12.2 星级（每关 3 档门槛）

**`target_money` 既是"过关线"也是"1 星"门槛**（已与玩家确认），
另外两个门槛是 `star2_money` / `star3_money`：

| 关 | 1★（= 过关线） | 2★ | 3★ | 3★ 强度（元/秒） |
|---|---|---|---|---|
| 1 试营业 | 60 | 70 | 80 | 0.89 |
| 2 午市 | 80 | 90 | 100 | 0.83 |
| 3 晚高峰 | 90 | 100 | 110 | 0.92 |
| 4 周末 | 100 | 110 | 120 | 0.92 |
| 5 爆单日 | 120 | 130 | 140 | 0.93 |

> **本轮（2026）定稿**：1 星统一 0.67 元/秒；三星强度 **0.83~0.93 元/秒**，
> 而玩家实测上限约 **1.0 元/秒** —— 从 L2 起三星基本压在实测上限的 83%~93%，
> L5 最紧（0.93）。L4 的 `time_limit` 同时从 120 放宽到 **130 秒**。

**判定规则**（`Game.stars()`）：从高往低比，第一个够到的门槛就是所得星级。

| 营收 | 星级 | 结算界面 |
|---|---|---|
| < 1★ | **0 星** | 标题「通关失败」，**不画星星** |
| ≥ 1★ | 1 星 | ★（黄）+ ☆☆（白） |
| ≥ 2★ | 2 星 | ★★（黄）+ ☆（白） |
| ≥ 3★ | 3 星 | ★★★（全黄） |

- **星星画在「通关成功」四个字下面**，居中；
- **黄的在前**（第 1..N 颗黄、其余白）；
- 0 星时**整行隐藏**（已与玩家确认，不画 3 颗空心星）；
- **HUD 的星级目标在顶部正中的 `TargetBar` 面板里**：三颗星按 **1★→3★**
  从左到右（金额递增），每颗星下面是 `当前/目标`（如 `45/60`），
  达到哪一档那颗星就变金黄实心；`第 N 关 · ⏱ 倒计时 · 钱` 留在**左上角状态栏**第一行
  （本轮已去掉「还差 N 元」，玩家要求）。
  尺寸：星 40px、数字 16px、整块约 176×77，收在后厨窗口上沿（y=96）以上；
  `mouse_filter = IGNORE`，不挡场景点击；
- 星星是**代码画的多边形**（`scripts/ui/star_icon.gd`），不是字符 ★ 也不是贴图：
  这个工程的默认字体连汉字都要回退，用字符风险太大；多边形完全可控，
  以后要换贴图只改这一个文件。**HUD 与结算界面复用同一个控件**。
- 回归：`tools/check_hud_target.tscn`（位置/尺寸/不挡家具/点亮逻辑，13 项）。

> **【数据校验】三档门槛必须严格递增**（1★ < 2★ < 3★）。
> 写反了不会报错，而是**判定结果毫无意义**（赚 60 元可能"同时是三星又没达标"）。
> 所以 `Config._load_levels()` 加载时就纠正并记进 `load_errors`。

**倒计时在弹窗期间照走**（用户明确要求）：现实里客人不会因为你思考而等你。
这条与「弹窗时世界暂停」**不冲突**，因为它们是两件事：

| | 世界推进 | 本关倒计时 |
|---|---|---|
| 正常游戏 | ✅ | ✅ |
| 打开后厨 / 饮料机弹窗 | ❌（既有规则） | **✅ 照走** |
| 开始界面 / 结算界面 | ❌ | ❌ |

> **【实现要点：倒计时不能放在 `Level._process` 里】**
> 弹窗会把整棵树 `paused = true`，而引擎**不给暂停树里的节点发 `_process`** ——
> 放在 Level 里的计时代码在弹窗期间根本不会执行（实测过）。
> 所以倒计时由 **`UIManager._process`** 推进（它是 `PROCESS_MODE_ALWAYS`），
> Level 只负责「世界该不该推进」。
>
> **【`run_active` 是唯一的「本局在进行吗」判据】**
> 不能用 `get_tree().paused` 代替：弹窗期间树是暂停的，但本局仍在进行、
> 时间仍在扣。也不能只看 `time_left > 0`：标题画面时它是初始值（满的）。

**开局与重开（`Level._start_level_run()`）**：清客人 → **`Table.reset_to_clean()`**（桌子回干净无单）→
`TableRules.clear_all_cleaning()` → 服务员归位并解锁 → **`router.clear_hand()`** → **`Game.reset_progress()`** →
`Game.start_level()`。

> **【换关必须"数据 + 视觉"一起清】** 玩家实测报过两个 bug，根因是同一类错误：
> ① **小人手上还端着上一关的餐品**（图标不消失）—— 只清了 `Game.hand`（数据），
> 而方块画的是 `waiter.carried`（视觉源），要靠 `router.clear_hand()` 里的
> `_sync_carried()` 才会同步；
> ② **脏桌进了新一局还显示是脏的**（看着像延迟 2 秒）—— 直接写
> `t.state = CLEAN_EMPTY` 只改数据，桌子的颜色/脏点全靠 `_refresh_look()` 里的
> `queue_redraw()`，所以视觉没跟上，要等鼠标动一下才重绘。
>
> **凡是"重置某样东西"，都要问一句：它的视觉是谁刷的？**
> 项目里这一类坑已经出现三次了（出餐口方块、脏桌、服务员手上标记），
> 病根都是"视觉更新搭在鼠标事件或某个信号的便车上"。
>
> **【这里踩过一个大坑：不能用 `Game.reset()`】**
> `reset()` 会连 **`tables` 注册表一起清空**，而桌子只在 `Level._ready()` 里
> 注册一次 —— 于是「点开始游戏」之后 `Game.tables` 一直是空的，
> **客人再也不生成**（实测：进游戏 10 秒 0 位客人）。
> 现在拆成两个函数：`reset()`（整局重建，含桌子注册表，只在搭场景时用）
> 与 `reset_progress()`（只清钱/接待数/手/后厨，重开一局用）。

---

## 13. 关键数值（`data/config.json`）

| 键 | 值 | 含义 |
|---|---|---|
| `table_count` | 3 | 桌子数 || `patience_waiting_to_order` | 28.0 | 等点单耐心（秒） |
| `patience_waiting_for_food` | 45.0 | 等上菜耐心（秒） |
| `patience_eating` | **10.0** | 用餐时长（秒）**【偏离：文档 12.0】** |
| `kitchen_cook_time` | 3.0 | 后厨每道菜耗时（秒） |
| `counter_capacity` | **2** | 出餐口容量（已从 1 改成 2，见 §5.1） |
| `customer_spawn_check_interval` | 1.0 | 客人检测间隔（秒） |
| `empty_table_delay` | 2.0 | 「空桌出现超过 2s」从桌子最近一次变为「干净且无人」算起 |
| `waiter_speed` | 290.0 | 服务员速度（px/s） |
| `clean_time` | **2.0** | 收拾耗时（秒）**【偏离：文档 3.0】** |
| `max_items_per_customer` | 2 | 每位客人最多点几样 |
| `hand_capacity` | **2** | 玩家手上能同时拿几份 |
| `item_count_weights` | `{1:0.5, 2:0.5}` | 客人点 1 样 / 2 样的概率 |
| `group_size_weights` | `{1:0.7, 2:0.3}` | 一组来 1 人 / 2 人的概率 |
| `min_main_per_group` | **1** | 每桌至少几份主食（0 = 关闭），见 §4.1 |
| `group_order_reroll_max` | 20 | 整组主食不足时重摇次数上限（防死循环） |
| `group_entry_stagger` | 36.0 | 同组客人在门口的 x 轴错开量（px），见 §4.2 |
| `customer_speed` | 220.0 | 客人走路速度（px/s，文档未写，补充项） |
| `stuck_timeout` | 2.0 | 卡住多久后放弃指令（保底防死锁） |

**代码内常量**（不在 config）：

| 常量 | 值 | 位置 |
|---|---|---|
| `WAITER_RADIUS` | 16.0 | `level.gd` |
| `ARRIVE_EPS` | 6.0 | `waiter.gd` |
| `GOAL_TOLERANCE` | `RADIUS + 3` | `waiter.gd` |
| `Cell` | 20.0 | `pathfinder.gd` |
| `CUSTOMER_INFLATE` | 8.0 | `pathfinder.gd` |
| 客人 `RADIUS` | 15.0 | `customer.gd` |
| 客人 `TOUCH_RADIUS` | 50.0 | `customer.gd` |
| `WALL_H` | 96.0 | `floor_art.gd` |
| `MAX_STICKERS` | 5 | `constants.gd` |
| 开始界面标题字号 | 56 | `start_screen.gd` |
| 开始界面按钮 | 240×64 / 24px | `start_screen.gd` |
| 开始界面遮罩 | `alpha 1.0`（纯黑） | `start_screen.gd` |

---

## 14. UI

### 14.1 布局（全部显式摆位，**不用锚点**）

| 区域 | 位置 | 内容 |
|---|---|---|
| 左上 | `(20, 20)` 起 | **只有一行**：`第 N 关 · ⏱ m:ss · 钱 N 元`（剩 10 秒内倒计时转红） |
| 顶中 · 目标栏 | 水平居中、`y=16` | **三颗星**（1★→3★，目标金额递增）+ 每颗星下面 `当前/目标`；达标那颗变金黄。约 176×77，收在后厨上沿（y=96）以上 |
| 左上 · 订单栏 | `(20, 88)` 固定 | 每桌一张票（桌号 + 菜名色块 + 名字 + 划线/还差N） |
| 左下 · 手上 | `y = 视口高 - 96` | 「手上：空手」/「手上：汉堡、可乐」 |
| 开始界面 | 独立 CanvasLayer（`layer + 2`） | 见 §14.6 |
| 弹窗层 | `layer + 5` | 后厨 UI / 饮料机 UI + 遮罩 |

> **本轮（2026，玩家要求）删掉了左上角那 7 行数据**（钱 / 已接待人数 / 空桌数 /
> 待收拾数 / 在店人数 / 出餐口 / 后厨队列）和那条分隔线，只留「第 N 关 · 倒计时 · 收入」。
> 这些数值仍然在 `Game.hud_snapshot()` 里算着（工具与测试还在用），只是不上屏。
> 因为状态栏高度固定了，**订单栏从「按状态栏高度推算」改成固定 `y=88`** ——
> 顺带删掉了那套「高度是 0 就 `call_deferred` 自己」的重试逻辑
> （它曾经是个无限续期的崩溃源）。
>
> **状态栏原来还有一行大号标题「餐厅物语」，更早就删了**（游戏名只在开始界面出现）。
> 回归：`tools/check_hud_target.tscn`（18 项，含「七行确实不在」「订单栏固定在 88」）。

> **坑**：`CanvasLayer` 的 Control 子节点**不会自动撑满视口**。父尺寸为 0 时锚点全部退化到原点
> —— 「手上：空手」会被算到 `y = -54`，安静地跑到屏幕外，不报错也不警告。
> 而且 `set_anchors_preset` 只改锚点、**不会**把 0 尺寸节点撑开。
> 所以 `UIRoot` / `HUD` / 弹窗 / 开始界面全部显式摆位。

### 14.2 订单票的显示规则

| 情形 | 显示 |
|---|---|
| 该菜**全部**送完 | 划红线 + 灰色 |
| 重复菜品**部分**送完 | **不划线**，显示 `可乐×2` + 右侧「还差2」，文字转暗黄 |
| 单份菜送完 | 划红线 |

> **已修的 bug**：原来把「送了一部分」也划了红线（`done or (total>1 and left<total)`），
> 于是「点 3 杯可乐、只上 1 杯」也立刻被划掉。文档只要求「**已送达**的菜划红线」，
> 部分送达的正确表现是**减数量**。

### 14.3 客人与服务员头顶

| 显示 | 时机 |
|---|---|
| 耐心环 | `WAITING_TO_ORDER` / `ORDER_TAKEN` / `ORDERED` |
| `!` | `WAITING_TO_ORDER` |
| 「用餐中」气泡 | `EATING` |
| 餐品气泡（**已移除**） | — |

> **菜名只在左侧订单票上显示，客人头顶不再显示菜名**（已与用户确认）。
> 理由：玩家的决策链是「看票决定做什么菜 → 送到哪一桌」，气泡不参与决策；
> 同桌共享订单下「谁点了哪份」在玩法上不存在（自动分配）；两个真相来源容易不一致；
> 而且会和耐心环 / `!` 互相遮挡。

**服务员手上标记**：最多 2 份 → **左肩 + 右肩对称**两个方块
（第 1 份在右肩 `(15, -13)`，第 2 份在左肩镜像 `(-15, -13)`）。方块而非圆形，
是为了和「服务员是圆的」区分开。

### 14.4 飘字

- 最多 **5** 条（`MAX_STICKERS`）。
- 文案集中在 `constants.gd`：请先点菜 / 这桌没点这个菜 / 手上已有餐品 / 手上没有餐品 /
  出餐口没有餐 / 过不去 / 你拿不下其他东西了 / 用餐中 / **这桌是空的**。

### 14.5 字体

Godot 默认字体**不含汉字**。`scripts/ui/ui_font.gd` 先试 `assets/ui_font.tres`，
失败则回退系统 CJK 字体（微软雅黑 / SimHei / Noto）。
启动时那行 `Error loading custom project font` 是无害的（引擎初始化主题的时序问题）。

### 14.6 开始界面（`scripts/ui/start_screen.gd`）

**游戏启动先停在标题画面，点了「开始游戏」才进入游戏。**

| 项 | 值 |
|---|---|
| 内容 | 上：`麦麦快餐店`（56px、金黄 `COLOR_MONEY`）；中：`成为麦麦快餐店的服务员，燃烧你的牛马之魂吧！`（18px、暗色）；下：`开始游戏`（240×64、24px） |
| 位置 | 三块作为**整体**在屏幕正中间（`CenterContainer`，不自算坐标） |
| 背景 | **纯黑不透明**遮罩（`alpha = 1.0`）—— 背后的餐厅画面一点不露 |
| HUD | **隐藏**（`UiManager.begin()` 显式设 `hud.visible = false`） |
| 世界 | **冻结**（`get_tree().paused = true`）：客人不生成、后厨不计时、飘字不衰减 |
| 层级 | 独立 `CanvasLayer`（`layer + 2`）：盖住 HUD，但仍在弹窗层（`layer + 5`）之下 |
| 文案常量 | `Constants.START_TITLE` / `START_SUBTITLE` / `START_BUTTON` |

**「出不出开始界面」的判据在 `level.gd` 的 `_ready` 里同步算**，两条都成立才算入口：
`get_parent() == get_tree().root` **且** `get_tree().current_scene == self`。

> **【不要在 main.tscn 里写 `show_start_screen = true`】**
> 那是上一版的写法，后果很严重：**任何**加载 `main.tscn` 的脚本（含全部 18 个工具）
> 都会弹开始界面 → `UI.begin(true)` → **整棵树被暂停**。
> 而引擎**不给暂停树里的节点发 `_process`**，于是靠 `_process` 跑逻辑的测试
> 会被**静默冻死**（实测：客人生成器一次都没跑，工具却照样打印「全部通过」）。
> `show_start_screen` 字段仍然保留，但只作为**工具强制指定**用的开关
> （想测开始界面的工具在 `instantiate()` 后自己设 true）。
>
> **【为什么要两条判据】** 单看任一条都有反例：
> ① 只看「父节点是 root」—— `run_tests.gd` / `scan_reach.gd` / `diag_detour.gd`
> 是 `get_tree().root.add_child(level)`，父节点**就是** root，会被误判；
> ② 只看 `current_scene == self` —— 实测引擎在 `main.tscn` 启动时
> `_ready` 内读到的**就是** Level（这条本身成立），但工具把 Level 挂到
> 自己节点下时 `current_scene` 指向工具、这条会挡掉，而 ① 挡不掉。
> 两条合起来：主场景两个都成立；上述三个工具只满足 ①，被 ② 挡掉。

**点「开始游戏」那一刻做三件事**（`UIManager.begin_game()`）：

1. 收起界面 + **恢复 HUD**（隐藏和恢复是两个出口，缺一处就会出现「进游戏后没有 HUD」）；
2. 解除冻结（`paused = false`）；
3. **把「空桌计时」归零**（`Game.reset_table_availability()`）。

> **【第 3 条为什么必须有】**
> 「空桌出现超过 `empty_table_delay`(2s) 才能坐人」（§16）算的是
> `Time.get_ticks_msec()` 的差值 —— **墙上时钟，场景树暂停它也照走**。
> 桌子是在建树时就 `mark_available()` 的，不归零的话：玩家在标题画面犹豫 30 秒，
> 一点开始三张桌子都「早就空够 2 秒了」，客人几乎瞬间涌进来，等于没有准备时间。

> **【开始界面的按钮能被点到，是因为 `process_mode = ALWAYS`】**
> 暂停期间 `PROCESS_MODE_INHERIT` 的节点收不到输入。`UIManager` 与其下的
> 开始界面 / 弹窗全部是 `ALWAYS`（§17），否则「开始游戏」按钮按不动，
> 游戏会永远卡在标题画面上。

---

## 15. 点击逻辑

`ClickRouter.handle_click(world_point)`：

```
1. 树暂停中（弹窗）→ 直接返回
2. UI 有弹窗 → 直接返回
3. 服务员 _locked（正在收拾）→ 直接返回（文档第九节：不可取消）
4. **先找客人**（客人身体和桌子矩形重叠，必须先判）
     命中条件：state 可点击 且 距离 ≤ click_radius(RADIUS+6)
     多个命中时取**最近的**
     → customer.interact()
5. 再找家具（第一个 `accepts_click()` 为真的）
     COUNTER → 按点在小矩形还是大矩形分派：
                 点在出餐口小矩形 → take_from_counter()
                 否则             → open_kitchen()
     其他    → obj.interact()
6. 什么都没点到 → walk_to_only(point)（只是走过去）
```

**铁律（文档第十八节）**：所有动作在**到达目标点时必须重新检查条件**。
每个动作都拆成两半：

| 阶段 | 方法 | 检查什么 |
|---|---|---|
| 点击瞬间 | `interact()` | 够不够格下单 |
| 到达瞬间 | `_do_xxx()` | 这段时间世界可能变了 —— 返回 false 则动作取消，服务员停在原地 |

**为什么不在点击时直接执行**：因为「跑腿」就是这个游戏的压力来源，点击立刻生效 = 没有难度。

**走路失败必须有提示**：`ClickRouter.watch_waiter()` 统一订阅 `command_cancelled`，
所有指令（接单 / 收拾 / 取餐 / 去后厨…）自动都有飘字说明，不会出现「点了没反应」。

---

## 16. 每帧逻辑

| 节点 | 帧函数 | 做什么 |
|---|---|---|
| `Level` | `_process` | ① 更新所有家具的悬停高亮（弹窗 / 收拾中则禁用）② 若 `UI.should_process_world()`：`Game.update_kitchen(delta)` ③ 每 **0.1s** 推一次 HUD 快照 |
| `Waiter` | `_physics_process` | `_active` → `_walk()`；否则 `cleaning_table != null` → `_advance_cleaning()`；每帧 `queue_redraw()` |
| `Customer` | `_process` | 按 `state` 分支：`WALKING_IN` 沿拐点走；三个等待态 `patience -= delta`（≤0 → 气走）；`EATING` 同理（≤0 → 吃完）；`LEAVING`/`ANGRY_LEAVING` 走向门口，到达时 `member_vacated()` + 发 `gone` |
| `CustomerSpawner` | `_process` | 每 `customer_spawn_check_interval`(1s) 尝试生成一组客人 |
| `Kitchen` | 由 `Level` 驱动 | 见 §5 |

**客人生成规则**（`customer_manager.gd`）：

1. 全店同时只允许 **1 位**客人处于「走向座位」状态；
2. 随机组大小（1 人 70% / 2 人 30%）；
3. 必须能找到**同一张**能容纳整组的空桌（干净 + 无人 + 空桌时间 ≥ `empty_table_delay`）；
   找不到 → **不拆组，继续等**；
4. 组内成员**依次**入场（前一位落座后下一位才生成）。

---

## 17. 暂停规则

**暂停由 `UIManager` 拥有**（`get_tree().paused = true`）。

| 触发 | 效果 |
|---|---|
| **显示开始界面（开局）** | 暂停：世界完全不推进 —— 见 §14.6 |
| 打开后厨 UI / 饮料机 UI | 暂停：世界不推进（后厨计时、客人耐心） |
| 关闭弹窗 / 点「开始游戏」 | 恢复 |

- `Level._process` 用 `UI.should_process_world()` 挡住 `Game.update_kitchen()`；
- `ClickRouter.handle_click()` 在 `paused` 时直接返回 —— 弹窗 / 开始界面期间场景不接受交互；
- 悬停高亮在弹窗期间全部禁用；
- **暂停期间能点的东西只有 `PROCESS_MODE_ALWAYS` 的 UI**（开始界面的开始按钮、
  弹窗的完成 / 取消）。这条不成立就会出现「按不动、永远卡住」。

---

## 18. 「到达时再检查」规则

文档第十八节的铁律。汇总当前所有动作的**两阶段检查**：

| 动作 | 点击时检查 | 到达时再检查 |
|---|---|---|
| 接单（点客人 / 桌） | 客人还在 `WAITING_TO_ORDER` | 状态未变；桌子有效 |
| 上菜（点客人 / 桌） | 手上非空；手上有该桌缺的 | 手上**现在**还有该桌缺的 |
| 取出餐口 | 手上还有空位；出餐口有餐 | 同上两条 |
| 去后厨 | 无 | 无（打开 UI） |
| 饮料机 | 手上还有空位 | 同上 |
| 收拾 | 桌子仍 `DIRTY`；服务员未锁 | 同上 |
| 垃圾桶 | 手上非空 | 手上非空 |
| 点空地 | 无 | 无 |

**失败表现**：handler 返回 `false` → 动作取消，服务员停在原地，
`command_cancelled` 让 `ClickRouter` 飘字说明原因。

---

## 19. 配置文件

### 19.1 `config.json`

见 §13。`_` 前缀的键是注释，代码忽略。
**关卡（时长 / 过关线 / 星级门槛）不在这个文件里** —— 见 `data/levels.json`（§12.1 / §12.2）。

### 19.2 `menu.json`

```json
{
  "kitchen_items": [
    { "id": "burger",  "name": "汉堡",   "price": 15, "color": "c0392b", "main": true },
    { "id": "fries",   "name": "薯条",   "price": 6,  "color": "f2c230", "main": true },
    { "id": "chicken", "name": "炸鸡块", "price": 12, "color": "b5651d", "main": true }
  ],
  "drink_items": [
    { "id": "cola",  "name": "可乐", "price": 3, "color": "3f6fb5" },
    { "id": "juice", "name": "橙汁", "price": 5, "color": "e8811f" },
    { "id": "lemonade", "name": "柠檬水", "price": 2, "color": "f5e663", "start_level": 3 }
  ]
}
```

`id` 必须唯一 —— 它同时是**资源名**（`assets/items/<id>.png`）和**占位色键**。

`main` 是**可选字段**：`true` = 主食（参与 §4.1 的「每桌至少 N 份主食」校验），
饮品写了也无效（`is_drink` 的一律不是主食）。
**缺字段时按 `true` 处理**（`Config.MAIN_BY_DEFAULT`）—— 这样旧菜单文件
不会因为漏了一个字段就让整条规则静默失效；要排除某道菜请显式写 `"main": false`。

`start_level` 是**可选字段**（默认 1）：从第几关开始出现。
菜单是**全局**的，用这个字段表达「渐进解锁」，而不是给每关各写一份菜单
（那样"加一道菜"要改 N 处）。详见 §20.3 的「菜单按关卡解锁」。

### 19.3 `layout.json`
见 §1。所有 `_` 前缀键是注释（坐标约束、站位要求的说明）。

**站位点（`walk`）必须落在碰撞盒外面**：家具是 `StaticBody2D`，
目标点在碰撞盒内时 `move_and_slide` 会在最后几像素把服务员挡在外面，指令永远走不完。

**但也不能给太多余量**（玩家反馈「离饮料机还有很远就能点饮料，点餐台也一样」）：
站位点离物件视觉外框太远时，服务员会停在机器下方老远就弹 UI —— 功能全对、不报错，
只是观感不对。定值公式：

```
walk.y = 碰撞盒下边 + 身体半径(16) + 4px 视觉间隙
```

| 物件 | walk | 碰撞盒下边 | 离视觉外框 |
|---|---|---|---|
| 后厨 | `540, 188` | 168（=168+20） | 0px（正好贴住柜台下沿） |
| 出餐口 | `540, 188` | 同上 | 0px |
| 饮料机 | `840, 191` | 171（=171+20） | 5px |
| 垃圾桶 | `270, 660` | — | 30px（绕着桶站，未改） |
| 门口 | `620, 600` | — | — |

> 实测站位由两处共同决定：这里的锚点，以及 `waiter.gd` 的
> `GOAL_CLEARANCE`（落脚点净空 = 17px）。最终服务员停在碰撞盒外 **26px**
> （身体半径 16 → 只差 10px，不重叠），视觉上就是「贴着柜台站」。
>
> 回归断言：`check_touch_zone.tscn` 里「站位距离 ≤ 24px」（垃圾桶 32px 例外）。

### 19.4 `waiter.command_move()` 的**赋值顺序**（踩过的坑）

```gdscript
# 正确：先更新本次指令的目标，再算落脚点
_target = walk_point
_touch_target = touch_target
...
var new_goal := _compute_goal()
```

**为什么顺序不能反**：`_compute_goal()` 读的是 `_target` / `_touch_target`
（也就是**当前**指令的目标）。如果先算后赋值，它读到的是**上一条指令**的目标，
于是新指令把「上一个落脚点」当成自己的落脚点 ——
而服务员此刻往往正停在那个旧落脚点附近（< `ARRIVE_EPS`），
下一帧的到达判定立刻为真，**原地就把到达回调执行了**。

玩家看到的现象：离饮料机/点餐台还有半个屏幕，UI 就弹出来了；
而且只在「服务员正忙时点击」才复现（闲时他本来就在附近，看不出问题）。

回归：`check_kitchen_flow.tscn` 第 4 组（走真实 `handle_click` 链路，
断言弹窗打开时服务员离目标 ≤ 24px）。

### 19.4 `assets/ui_font.tres` / `assets/items/`

- 字体：见 §14.5。
- 餐品图：把图片按 **`id`** 命名放进 `assets/items/`（支持 `.png` / `.webp` / `.jpg`），
  游戏自动使用；找不到就退回 `menu.json` 的 `color` 色块。**可以一张一张慢慢换。**

---

## 20. 扩展性说明

### 20.1 改数值不用碰代码

| 想改什么 | 改哪里 |
|---|---|
| 菜价、加菜、加饮品 | `data/menu.json` |
| 耐心、备餐时间、速度、容量、手上能拿几份 | `data/config.json` |
| 家具位置、加桌子、出餐口大小 | `data/layout.json` |
| **关卡数量、每关时长与营收目标** | `data/levels.json` |

### 20.2 常见扩展的做法

| 想做什么 | 怎么做 |
|---|---|
| **加一道后厨菜** | `menu.json` 的 `kitchen_items` 加一条 → 后厨 UI 自动多按钮、订单栏自动显示、菜价自动读取 |
| **加一种饮品** | 加进 `drink_items` → 饮料机 UI 自动多按钮（要"第 N 关才出现"就写 `start_level`） |
| **加一张桌子** | `layout.json` 的 `tables` 加一条（含 `seats`）→ `level.gd` 自动建节点并注册 |
| **调星级门槛** | `levels.json` 的 `star2_money` / `star3_money`（1★ 就是 `target_money`） |
| **手上能拿 3 份** | `config.json` 的 `hand_capacity` 改 3（标记与 HUD 都是按列表长度渲染的） |
| **换餐品美术** | 图片按 id 放进 `assets/items/`，或填 `ItemArt.override` |
| **换家具 / 客人 / 服务员美术** | 改各自脚本的 `_draw()`；`floor_art.gd` 换成 Sprite2D |

### 20.3 统一入口（改这些地方时只改一处）

| 关注点 | 唯一入口 |
|---|---|
| 手上容量判断 | `Game.hand_has_room()` / `hand_is_full()` |
| **重开一局清什么** | `Game.reset_progress()`（**不要用 `reset()`**，它会清掉桌子注册表） |
| **换了 Kitchen 之后谁要重绑** | `Game.kitchen_changed` → `KitchenWindow._rebind_counter()` |
| **本关是否在进行** | `Game.is_run_active()`（不能用 `get_tree().paused` 代替，见 §12.1） |
| **倒计时推进** | `UIManager._process`（`PROCESS_MODE_ALWAYS`，弹窗期间照走） |
| **桌子能否坐人** | `TableRules.is_seatable()`（排除「正在收拾」+ 要求 `CLEAN_EMPTY`） |
| **上菜从手上扣几份** | `Customer._deliver_items()`（唯一真相 = `Table.assign_delivery()` 的返回值） |
| **菜单能否按关卡解锁** | `Config.item_available_at()` / `drink_items_at()` / `pool_at()`（读 `start_level`） |
| 出餐口内容写入 | `Kitchen._set_counter()`（必定发 `counter_changed`） |
| 餐品外观 | `ItemArt.draw_swatch()` / `make_node()` / `texture_for()` |
| 桌子能否变脏 | `Table.member_vacated()` 里的 `state != OCCUPIED` 判断 |
| 收拾完成后桌子变什么 | `Table.finish_clean()`（有人在座则保留 `OCCUPIED`） |
| **重开一局时桌子怎么复位** | `Table.reset_to_clean()`（**不要直接写 `state`**，那样不刷新视觉） |
| **换关时手上餐品怎么清** | `ClickRouter.clear_hand()`（**不要用 `Game.clear_hand()`**，它不同步服务员身上的标记） |
| 结账金额 | `Table.bill_amount`（`open_order()` 时算好） |
| 接触几何 | `touch_box()` / `touches_from()` / `touch_point()` / `standoff()` |
| 点击路由 | `ClickRouter.handle_click()` |
| 走路失败提示 | `ClickRouter.watch_waiter()` |

### 20.4 加新可交互对象时的检查清单

1. 继承 `EntityBase`（或实现同等的 `hit_test` / `interact` / `accepts_click` / `walk_to`）；
2. 在 `layout.json` 加坐标，在 `level.gd` 里建节点；
3. `walk` 站位必须落在**碰撞盒外面**；
4. **`interact()` 与 `_do_xxx()` 两阶段检查都要写**；
5. **把它的点击路径加进测试**（`ClickRouter.handle_click()` 是公开的，用它模拟真实左键 ——
   直接调内部方法会绕过玩家真正走的那条路，等于没测）；
6. 在 `constants.gd` 加文案常量，别在代码里写裸字符串。

### 20.5 场景为什么由代码搭

1. 坐标本来就在 `layout.json`，手写 `.tscn` 等于把数字抄第二遍，
   两份不一致就会出现「点得到但走不到」这种极难查的 bug；
2. 加桌子只要改 JSON；
3. 脚本里的结构可读、可 diff，二进制式的场景改动不可读。

### 20.6 调试开关

在工程根目录建一个**空文件 `DEBUG_WALK`** → 打开诊断日志（删除即关闭，关掉时零开销）：

- 每次寻路：起点、落脚点、A* 原始路径、整理后路径，**主动标出斜线段**；
- 客人踩到桌面时：坐标、座位、剩余拐点、该桌桌面矩形；
- 换落脚点时：原落脚点为什么够不到、改到哪里；
- 清洁开始时：服务员位置、可碰区、离它多少、这次是因为哪条判据算到达、累计卡住多久。

> 这个开关的存在理由：多次出现「我的测量和玩家看到的现象冲突」，
> 与其对着代码推断，不如把现场打出来。

---

## 附：工具清单（`tools/`）

| 脚本 | 用途 |
|---|---|
| `check_scripts.gd` | 逐文件 `load`，定位「哪个脚本编译不过」（**改完代码先跑这个**） |
| `check_touch_zone.tscn` | 碰桌子才触发（判定区 / 椅子外侧 / 双次点击） |
| `check_routes.tscn` | 每件家具的站位从服务员出生点**真走一遍**，报告能否到达 |
| `check_walk_around.tscn` | 绕行、贴边距离、路径长度 |
| `check_ortho.tscn` | 直角折线，**不许有斜线段**（⚠️ 当前 9/17 —— 8 项「服务员没走到落脚点」是**改动前就存在**的问题，见 PROGRESS.md §5.1） |
| `check_angry.tscn` | 气走不留脏桌 / 正常吃完变脏 |
| `check_customer_walk.tscn` | 客人走向座位会不会穿过桌子 |
| `check_hand2.tscn` | 手上 2 份的全部规则 |
| `check_money.tscn` | 结账金额与时机 |
| `check_ticket.tscn` | 订单票划红线规则（逻辑层 + 像素层） |
| `check_counter.tscn` | 出餐口方块像素级消失 |
| `check_round3.tscn` | 第二轮优化的回归 |
| `check_standoff.tscn` | 服务员与客人的距离（不重叠） |
| `diag_clean.tscn` | 清洁位置取证（含双次点击场景） |
| `dump_nav.tscn` | 打印家具尺寸 / 标签是否放得下 / 导航网格 |
| `dump_ui.tscn` | UI 布局体检，打印每个控件的真实矩形 |
| `screenshot.tscn` | 离屏截图（姿势：`hand:burger+cola` / `kitchen` / `drink` / `dirty` / `order` / `touch:right`） |
| `run_tests.tscn` | 综合测试套件（**部分分组已过时，见 PROGRESS.md**） |
| `check_start.tscn` | 开始界面体检（31 项）+ 出两张截图 `start_screen.png` / `after_start.png` |
| `check_order_rule.tscn` | 炸鸡块 + 点单规则体检（40 项）：主食下限（8000 次大样本）、开关、无解兜底、同组同帧生成、**自动生成不会「来一组就停」** |
| `check_clean_seat.tscn` | 收拾与落座的冲突体检（33 项）：收拾期间不可坐、生成器选不到、`finish_clean` 守卫、不留永久标记 |
| `check_level_mode.tscn` | 关卡模式体检（158 项）：关卡表、开局初始化、**星级判定与星星显示**、倒计时（含**弹窗期间照走**）、结算、下一关/重试、换关不残留、出餐口订阅重绑 |
| `check_kitchen_flow.tscn` | 后厨出餐流程体检（44 项）：**一道一道做**（实测间隔 3.0s）、**不阻塞**（出餐口满了也把菜做完）、取走一份当帧补位、容量 1 的老行为不破、真实点击一次拿 2 份 |
| `check_serve_hand.tscn` | 上菜扣手体检（32 项）：手上 2 份只该扣 1 份、要 2 份就扣 2 份、不匹配的留着、**点桌子的 6 种提示文案与顺序** |
